# Resume after `/clear` — SandyStudio Phase 8 first real cycle

> **Open this file as the first message of the next session.** Phase 8 first-call shipped. Real providers verified end-to-end. Director is mid-test of E03 cycle (SS-S03-E01).

---

## TL;DR

**Phase 8 first-call MVP COMPLETE** (2026-04-30). Real provider stack verified end-to-end: gpt-image-1, Drive OAuth, persistBinary → Drive uploads, /settings/providers UI lets Director switch providers. Anthropic Haiku auto-generates Episode Brief on creation. Veo 3 wired but Director is currently testing it on live re-trigger.

The pipeline kebab + preview drawer + brief preview banner all polished and context-aware (Approve/Reject/Edit/Re-trigger only show when relevant; Edit→Preview branching by file type).

---

## What Director just tested (state of SS-S03-E01)

| What | Status |
|---|---|
| Brief (auto-generated by Anthropic Haiku) | ✅ APPROVED |
| Script (mock LLM) | ✅ APPROVED |
| Script QA (mock) | ✅ APPROVED |
| Storyboard (mock) | ✅ APPROVED (3 acts) |
| World Check (mock) | ✅ APPROVED |
| Animatic | ⏳ **EXEC-EDIT RUNNING with real Veo 3** — started 2026-04-30 18:41:36, polling every 5s, expected complete ~18:43-18:44 |
| Generation | not yet |
| Distribution | not yet |
| Publish | not yet |

Provider switches done in /settings/providers:
- **Storage**: drive_native (live)
- **Video**: veo-3 (live, Gemini billing enabled by Director)
- **Image**: gpt-image-1 (live, has been since first-call)

When Director refreshes the episode page in a few minutes, the animatic preview drawer should show real Veo MP4 + "Backed up to Google Drive" link.

---

## Architecture state — what is real vs mock

| Contract | Provider | Real / Mock |
|---|---|---|
| **Brief generation** (POST /api/episodes) | Anthropic Haiku 4.5 | ✅ real |
| **Image** (EXEC-THUMB) | gpt-image-1 | ✅ real |
| **Storage** (binaries) | Google Drive (`drive_native`) | ✅ real, verified via `npm run test-pipeline-drive` |
| **Video** (EXEC-EDIT animatic, EXEC-VGEN shots) | Veo 3 (Gemini API) | ✅ adapter ready, billing enabled, Director testing now |
| Text agents (EXEC-SW, SREV, SB, WCHK, COPY) | mockLLM | ❌ mock — text-LLM real wiring is a separate adapter (Sprint 10 candidate) |
| Music (EXEC-MGEN) | Beatoven | ⚠️ inactive, mock |
| SFX | ElevenLabs | ⚠️ inactive, mock |
| Publish (EXEC-PUB) | YouTube Data API | ⚠️ mock (wired last per spec) |

Cumulative cost so far: ~$0.045 (3× test-image runs + 1 brief generation) + $0.005 first Veo attempt that hit free-tier 429 + first real Veo run (~$0.38 in flight).

---

## Files shipped this session (Phase 8 first-call)

### Provider architecture
- `webapp/supabase/migrations/0014_provider_assignments.sql` — global tier table, 7 contracts seeded
- `webapp/supabase/migrations/0015_assets_drive_fields.sql` — `drive_file_id` + `drive_web_view_url` on assets
- `webapp/lib/agents/provider-resolver.ts` — DB lookup + 60s cache + auto-mock fallback when env key missing
- `webapp/lib/agents/persist-binary.ts` — local cache + Drive upload with graceful degrade
- `webapp/lib/agents/providers/google-auth.ts` — OAuth refresh-token flow
- `webapp/lib/agents/providers/drive.ts` — Drive API v3 (folder ensure + multipart upload + list + delete)
- `webapp/lib/agents/providers/openai-image.ts` — gpt-image-1 adapter
- `webapp/lib/agents/providers/veo-gemini.ts` — Veo 3 long-running operation pattern
- `webapp/lib/agents/providers/anthropic-brief.ts` — Episode Brief generator (Haiku 4.5)
- `webapp/lib/api/provider-catalog.ts` — UI catalog (Drive + Veo no longer "wip")

### UI
- `webapp/components/settings/ProviderSettings.tsx` — global tier dropdown + on/off + health badges
- `webapp/components/preview/PreviewDrawer.tsx` — right-side overlay, small/wide/full toggle
- `webapp/components/preview/AssetPreview.tsx` — md/img/video/audio render + Drive backup badge
- `webapp/components/editor/MarkdownEditor.tsx` — CodeMirror 6 wrapper
- `webapp/components/editor/EditorModal.tsx` — Edit modal with read-only mode for APPROVED/LOCKED
- `webapp/components/editor/RejectModal.tsx` — REQUEST_REVISION with note
- `webapp/components/ui/DropdownMenu.tsx` — minimal kebab primitive (no Radix dep)
- `webapp/components/pipeline/StageKebabMenu.tsx` — context-aware items (state-driven), text→Edit, binary→Preview, producer-only re-trigger

### API
- `webapp/app/api/providers/assignments/route.ts` — GET enriched
- `webapp/app/api/providers/assignments/[contract]/route.ts` — PUT with audit + cache invalidate (uses **service-role** client to bypass RLS)
- `webapp/app/api/assets/[id]/content/route.ts` — GET/PUT markdown body (DB column, not fs)

### Episode flow
- `webapp/app/api/episodes/route.ts` POST — builds template brief + tries Anthropic Haiku enrichment
- `webapp/app/(studio)/episodes/[id]/page.tsx` — banner with brief preview, Edit button, kebab integration, preview drawer

### Test scripts
- `webapp/scripts/test-image-provider.ts` — `npm run test-image` (~$0.011-0.04)
- `webapp/scripts/test-drive-provider.ts` — `npm run test-drive` (free)
- `webapp/scripts/test-video-provider.ts` — `npm run test-video` (~$0.38 fast)
- `webapp/scripts/test-drive-binary-pipeline.ts` — `npm run test-pipeline-drive` (E2E)
- `webapp/scripts/probe-failed.ts` — debug episode state from cli

---

## Active dev environment

```bash
# Two terminals (already running in background of prior session):
cd webapp && npm run dev          # localhost:3000
cd webapp && npm run inngest:dev  # localhost:8288
```

Director is logged in as `ostrovoy.alexander@gmail.com`. Supabase project ref: `akstennzrnkvexjgzhxv`.

---

## Bugs deferred (pending after Phase 8 first-call)

| # | Bug | Impact |
|---|---|---|
| 1 | Activity_events `agent_completed` not landing in feed | Director can't see "EXEC-SW completed" in Pipeline View activity. Asset still appears, just no event. Probably actor field mismatch or RLS on activity_events insert (need to inspect — might be same RLS pattern as provider_assignments was). |
| 2 | Stuck RUNNING jobs (multiple in SS-S03-E01) | Pipeline view shows misleading state. Need GC: mark CANCELLED after N min without completion. Probably Inngest dev-mode hot-reload kills mid-execution. |
| 3 | FAILED job overrides APPROVED in stage state precedence | Cosmetic — APPROVED asset visible but stage glyph shows X. Fix: APPROVED asset should win over stale FAILED job. |
| 4 | JSON-style content (REV-world_check verdict) edits in markdown editor without JSON syntax highlight | Minor polish — install `@codemirror/lang-json` and detect language from filename/content. |

---

## Next steps after /clear

When Director resumes:

1. **Refresh `/episodes/<SS-S03-E01-id>`** — animatic should now be done. Preview drawer shows real Veo MP4. **Verify Drive backup link present**.
2. If satisfied with animatic → **Approve** it via kebab → fan-out fires:
   - 3× EXEC-VGEN (per-shot real Veo, ~$1.20 total)
   - 1× EXEC-MGEN (mock — music provider still off)
3. After all 4 outputs APPROVED → Distribution stage:
   - EXEC-COPY metadata (mock)
   - EXEC-THUMB thumbnail (real gpt-image-1)
4. Approve Distribution → EXEC-PUB (mock, just logs)
5. **First real-provider end-to-end cycle complete** ✅

Optional: shut Veo back to mock for shot generation if budget-conscious. Real animatic alone is sufficient proof of concept.

### Sprint 10 candidates (in priority order)

1. **Activity events fix** (above) — visibility into pipeline
2. **Friendly agent names** (debt #1) — EXEC-SW → "Screenwriter" everywhere
3. **Anthropic adapter for text agents** — replace mockLLM in runner.ts EXEC-SW/SREV/SB/WCHK/COPY with Claude Sonnet calls (parallel structure to anthropic-brief.ts)
4. **Series Genesis workflow** (BOARD-MKT, Series Bible UI) — Director's vision from earlier in this session
5. **Concierge tools** — `create_episode_with_brief` tool wired to existing helpers
6. **Per-stage provider override** (Phase 8 step 9) — kebab "Provider › ▾" sub-menu
7. **PA-001/2/3 Character reference architecture** — Drive-based master refs for Veo img2vid

---

## Key memory facts (auto-loaded next session via MEMORY.md)

- [Director communication style](director_communication_style.md) — Russian, terse, ===5=== gates writes
- [Concierge uses OpenAI](concierge_uses_openai.md) — direct API not Anthropic
- [Webapp local dev — two terminals](webapp_local_dev_two_terminals.md)
- [Supabase project ref](supabase_project_ref.md) — `akstennzrnkvexjgzhxv`
- [replay-pilot harness](replay_pilot_harness.md) — `npm run replay-pilot`
- [No `npm run build` while dev is up](dev_workflow_no_build_during_dev.md)
- [Phase 8 provider strategy](sprint9_phase8_strategy.md) — Google-first MVP, two-tier UI switching
- [First real provider call](first_real_provider_call.md) — gpt-image-1 succeeded; 5-step adapter pattern reusable

---

## Verify gate

Last green:
- `tsc --noEmit` clean
- 79/79 unit tests pass
- 28/28 replay-pilot pass
- Real Veo job firing right now (in-flight at /clear time)

---

*Generated 2026-04-30 ~22:42 at /clear point. Replace this file at next checkpoint.*
