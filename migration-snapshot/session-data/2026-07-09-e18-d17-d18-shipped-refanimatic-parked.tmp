# Session 2026-07-09 — E18 PR-B shipped (D17/D18) + ref-animatic parked

**Project:** SandyStudio. **Branch:** `teo/e18-smoke` (worktree `fetch-timeout-swaps`).
**Директива:** выводы + следующие действия, не история.

## ⭐ СТАРТ ЗДЕСЬ для свежей сессии
Следующая работа — **PR-A дискретные фиксы**. Открыть `docs/analysis/E18-fix-plan.md`
§БАКЕТ 3. Реф-аниматик (D6/D7) ПРИПАРКОВАН — отдельный blueprint
`docs/analysis/E18-ref-animatic-removal-analysis.md` (первое решение на возврат =
развилка масштаба §6 + подтвердить «sequential мёртв»).

## Что залендилось (эта сессия)
1. **D17 — курирование feed Полины (два firehose, чистое вычитание).** Gate B
   (`event-actionable.ts`) → 8 MUST-WAKE. Gate A whitelist синхронизирован в 3 местах
   (SQL-триггер **миграция 0042 ПРИМЕНЕНА в прод**, `ambient-events.ts`, backfill).
   Fail-dedup: payload `pa/notify-needed` +actor/assetId; `exec-pa-react` `:fail`-бакет
   по (actor,asset_id). Фенс `CONCIERGE_AUTO_REACT_MAX_CALLS` 500→40 (`.env.local`).
   Эффект: 438→~30 инъекций, 378→~20 платных, **$18.82→~$1.30 (14×)**.
2. **D18 — учёт интерактива.** `chat/route.ts` пишет `recordConciergeCost(source:'chat')`
   на tool+streaming раундах; НЕ гейтит (Директор = escape hatch).
3. **Ref-animatic blueprint** написан (переосмысление: режем церемонию sequential,
   не класс; контракт `animatic_v1` несущий; D6 = ранняя материализация EDL; D7 исправлен).

## Коммиты
- `8666f48` fix(E18): D17 curate Polina feed + D18 meter interactive chat
- `ad93d6a` docs(E18): ref-animatic removal blueprint (D6/D7)
- (ветка **2 коммита впереди origin, НЕ запушена** — по запросу Директора «чекпойнт»)

## Verify (Ritual 3)
tsc **0** · vitest **1152/1152** · replay-pilot **30/30** · миграция 0042 applied+confirmed
(`migration list` → `0042 | 0042 | 0042`).

## Диспозиции Директора (свежий взгляд, 2026-07-09)
- **D14/D15 НЕ «самый острый»** — перезалить музыку + бесплатная кнопка финал-ката =
  дешёвый обход. Чинить, но не первым. (Тео переоценивал — снято.)
- **D4 = косметика** отображения статуса критик-строки (`REV-*` всегда REVIEW by design,
  вердикт в `metadata.verdict`). В бакете матрицы, не срочно.
- **D6/D7 (реф-аниматик)** — большой радиус, отдельно + свежая сессия (сделано: blueprint).
- Дискретные D1/D2/D3b — «понятные», делать по fix-plan.

## Открыто / next
1. **PR-A дискретные:** D1 (casting до writer), D2 (casting=Director-гейт Mode 1, CRITICAL
   деадлок), D3b (MGEN проходной), D14/D15 (music approve-reflow: file-move+расширение).
   ⚠️ Ещё НЕ исследованы глубоко — начать с investigation каждого.
2. **Push ветки** — Директор ещё не сказал; предложено.
3. Реф-аниматик — свежей сессией со стартом от blueprint-дока.
4. После дискретных + (позже) матрицы → измеряемый ре-смоук для подтверждения D17 14×.

## Env
Worktree `C:\SandyStudio\.claude\worktrees\fetch-timeout-swaps`, deps через junction,
`.env.local` скопирован (+ фенс 40). Supabase ref `akstennzrnkvexjgzhxv` (worktree
залинкован — скопировал `.temp/{linked-project.json,project-ref,pooler-url}` из master).
Сервера ОТКЛЮЧЕНЫ, `MECHANICS_AUTO_ADVANCE` OFF.
