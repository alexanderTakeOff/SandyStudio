# Session: 2026-06-05

**Started:** 2026-06-04 (gate-hardening) → ran deep into 2026-06-05
**Last Updated:** 2026-06-05 ~13:30 (Dubai)
**Project:** SandyStudio (C:\SandyStudio) — master-session orchestrator + Director's proxy to Polina
**Topic:** Shipped Gate-Hardening **Phase 2**; global **anti-additivity** rule+hooks; drove E02 **SH01/SH02** plan-driven videos to APPROVED (killed 3 mock-masked bugs live); fixed the **duration-override/false-attribution** systemic bug (q9); re-authored **SH03/SH04** plans to 3s; researched the **versioning/kebab UI** problem (3 agents) + greenlit subtractive fix; diagnosed + started fixing the **720p-vs-1080p resolution surfacing** bug.

---

## What We Are Building

SandyStudio = AI animation studio (Next.js + Supabase + Inngest, local-first; webapp/). The Gate-Hardening RFC made the video subsystem fail LOUD + pre-paid. Phase 1 shipped prior session; **this session shipped Phase 2** and exercised it live on E02 "The Tidy Tornado" (id `8b32b3e0-d012-4545-a834-85bc140a2707`), surfacing real latent bugs the mock hid. Then pivoted to Director's UI/versioning pain + a resolution-display bug.

HEAD=`1c1d1ab`, **30 commits ahead of origin, NOT pushed** (push gate pending Director go).

---

## What WORKED (with evidence)

- **Global anti-additivity** — `~/.claude/rules/common/anti-additivity.md` (reuse+subtract), settings.json UserPromptSubmit+Stop hooks (node cmd verified exit 0), memory + MEMORY.md.
- **Gate-Hardening Phase 2** — all units (tsc 0 / vitest 670 / replay-pilot 30): U1/I5 provider-contract `b8ef059`; U4/I9 critic auto-bounce `108e8ee` (critic-loop.ts, +6 tests); U2/I4+I6 VGEN fold `cff8007` (execVgenRun, −160 lines); EREF-fold `bb0669c`; U3/I3 drop EXEC-VANIM preflight `37a2390`; U5/I8 closed-as-covered.
- **E02 SH01+SH02 videos APPROVED** — ac8a8a6e / 69cb1334; standard 1080p, 40°, $2.722 each (cost = 1080p math); reachable Drive links. Plan-driven path proven.
- **3 mock-masked bugs fixed live (all pre-paid, no money lost):** gate.ts byteless-animatic preflight `35691fb`; budget.ts supabase.rpc unbound-this `0da453d`; q9 duration-lock `a9ed342`.
- **q9 duration-lock WORKS (verified live):** re-authored SH03 v2 + SH04 v3 came out **3s** (locked to animatic, not the old 5s), 1080p, standard, critic PASS, V14 didn't need to fire. False-attribution now honest (claims only brief-values). Director set animatic SH03/SH04 → 3s override (legit path).
- **Versioning UI research (3 code-explorer agents, complete)** — the reusable picker ALREADY exists (`CandidatesStrip`, EREFv2Sections.tsx:314-421, generic) + AnimaticPlayer hover-popover (TD-80). Fix is REUSE+WIRING, not new components.
- **Resolution metaPatch fix** `1c1d1ab` — execVgenRun now persists `resolution`+`plan_asset_id` + shows resolution in green-box trace. tsc 0.

---

## What Did NOT Work (and why)

- **Polina's verbal-approval gate blocks EVERY mutation** (TD-37 scoped-auth gap) — bypass via direct Inngest fires (`POST :8288/e/<INNGEST_EVENT_KEY>`) + direct-DB approve (Director delegated "одобри за меня").
- **Safety classifier** correctly blocked: demoting REVIEW plans (outside q8); re-firing paid render (Director's "WAIT" in context). Resolved on explicit Director go.
- **animator.md prompt cached for process lifetime** (`loadSystemPrompt`) — q9 prompt edit invisible until animator.ts touched (`83f805f` forces HMR reset). If a regen still emits old behaviour → restart dev.
- **Resolution 720p artifact** — the 1080p SH03 video showed "720p" in the drawer because metadata.resolution was MISSING (metaPatch gap) → regen form falls back to 720p default. Director's manual 09:34 regen likely went out at 720p (DOWNGRADE). Forward-fixed; the FORM default + the regenerate-video route still need fixing.
- **Node one-liners:** `cd /c/SandyStudio/webapp` first (relative .env.local); wrap async in `(async()=>{…})()` (top-level await + require = ERR_AMBIGUOUS_MODULE_SYNTAX).

---

## What Has NOT Been Tried Yet (NEXT WORK — Director greenlit ===5===)

**A) Versioning UI fix (GO given, subtractive, ~40-50 LOC, 3 files, 0 new components):**
- Wire `onPickAsset` + `onPrev`/`onNext` into the pipeline page's PreviewDrawer (`app/(studio)/episodes/[id]/page.tsx:449-454`) + the activity-feed Eye drawer — copy what `EpisodeTimelineSection.tsx:229-249,444` already does → unlocks the existing `CandidatesStrip` for free.
- Remove the `isVidShotCurrent` guard in `AssetPreview.tsx:103` so the sibling-version fetch + CandidatesStrip works for ALL types (group by metadata.shot_id for shot stages, by file_type for others). Data already loaded via `GET /api/episodes/[id]`.
- Mount `CandidatesStrip` in `StageWorkspacePanel.tsx` (no picker today).
- Root of "latest only": `pipeline-stages.ts:384` sorts by created_at, takes [0] across all shots/versions.
- Keep AnimaticPlayer popover for the timeline surface. Universal nav primitive = `onPickAsset → setPreviewAssetId`.

**B) Resolution fix remainder:** the regen FORM (ProviderControlPanel) defaults to 720p → make it default 1080p / read from asset metadata (kill the downgrade trap). Also the manual `regenerate-video` route should persist resolution like execVgenRun now does. Optionally backfill existing SH-videos' metadata.resolution (derive 1080p from cost $2.722).

**C) q11 (open):** SH04 plan v3 (3s/1080p/45°) still REVIEW awaiting Director approval. SH03 Director is now iterating by hand (edited-prompt regen at 720p). Director must decide orbit (45°/60° ok, or tighten SH03 to 40°). Check his 09:34 SH03 regen output (likely 720p) + offer to redo at 1080p.

**D) Deferred:** TD-37 Polina scoped-auth; dashboard/global activity feeds have no asset_id; migration 0038 (VID-animatic unique index); seedance 4s floor (3s animatic → 4s video); UI 4-block sticky-pipeline + de-modalize PreviewDrawer (earlier agent a4ef18cf scoped it, ~150-250 LOC). q9 PUSH (30 commits).

---

## Current State of Files (all committed at 1c1d1ab)

| File | Status | Notes |
|------|--------|-------|
| origin/master | BEHIND | 30 commits ahead, NOT pushed |
| webapp/lib/agents/critic-loop.ts + runner.ts | DONE | I9 cap |
| webapp/inngest/functions/exec-vgen.ts | DONE | fold + resolution/plan_asset_id metaPatch + trace |
| webapp/inngest/functions/exec-eref.ts, lib/agents/gate.ts, lib/budget.ts, providers/video-gen-multi.ts | DONE | Phase 2 + fixes |
| agents/exec/animator.md + lib/agents/runners/animator-critic.ts + animator.ts | DONE | q9 duration-lock + V14 + cache note |
| app/(studio)/episodes/[id]/page.tsx, components/preview/AssetPreview.tsx, components/pipeline/StageWorkspacePanel.tsx | NOT STARTED | versioning UI fix (A) — greenlit |
| ProviderControlPanel + regenerate-video route | NOT STARTED | resolution form/route fix (B) |
| E02 SH01/SH02 videos | APPROVED | ac8a8a6e / 69cb1334 |
| E02 SH03 plan v2 / SH04 plan v3 | REVIEW (3s/1080p) | Director iterating SH03 by hand; SH04 awaits approval |

---

## Decisions Made

- **Animatic = locked source of truth for duration** (q9). Designer can't override; Director changes via animatic director_overrides.
- **Versioning fix is REUSE, not new components** — CandidatesStrip exists + is generic; just wire it everywhere + drop the VID-shot-only guard. Director: "Это ровно та форма. GO ===5===".
- **Resolution must be persisted + surfaced** — missing metadata caused a 720p display + silent downgrade-on-regen.
- **Approve-on-behalf legit** when Director explicitly delegates.

---

## Blockers & Open Questions

- Polina gate (TD-37) — bypassed, real fix pending.
- "Тео передал 5s on SH03/SH04" in Polina's chat — Тео did NOT; dispatcher source unclear; deferred.
- q9 push (30 commits) — Director hasn't said go.
- Director's 09:34 SH03 hand-regen — likely 720p; needs check/redo at 1080p.

---

## Exact Next Step

Director chose between (a) finish versioning UI fix + resolution form fix NOW, or (b) /clear + resume fresh (this save). On resume: implement **A) versioning UI fix** — start by adding `onPickAsset={(id)=>setPreviewAssetId(id)}` (+ onPrev/onNext mirroring EpisodeTimelineSection) to the PreviewDrawer at `app/(studio)/episodes/[id]/page.tsx:449`, then remove the `isVidShotCurrent` guard at `AssetPreview.tsx:103` to generalize the version fetch, then mount CandidatesStrip in StageWorkspacePanel. Then **B)** fix the regen form's 720p default. Verify trio after each. Also resolve **q11** (SH04 approval + Director's 720p SH03 regen).

---

## Environment & Setup Notes

- Servers via preview: next-dev :3000 serverId `b56b2458-0d9a-40d8-beb8-d43ea9ef8d0a`; inngest-dev :8288 serverId `1e02523d-…`. HMR picks up .ts; **animator.md edits need a touch of animator.ts** (prompt cache). NEVER `npm run build` while dev up.
- Scripts: `cd /c/SandyStudio/webapp`; read `.env.local` for SUPABASE_SERVICE_ROLE_KEY + TEAM_CHAT_TOKEN + INNGEST_EVENT_KEY; wrap node async in `(async()=>{…})()`.
- Direct event fire (bypass Polina gate): `POST http://localhost:8288/e/<INNGEST_EVENT_KEY>` `{name,data}`. VANIM `sandystudio/exec-vanim/plan` {episodeId,shotId,revisionNote (NO duration number — q9 locks it)}. Render `sandystudio/exec-vgen/single-shot` {episodeId,shotId,planAssetId,quality_tier:'standard'}.
- Nudge Polina: `POST :3000/api/team-chat/post`, author='Тео', Bearer TEAM_CHAT_TOKEN, field `content`. pa-tail: `node scripts/pa-tail.mjs recent N` from `C:\SandyStudio\.claude\worktrees\quizzical-brown-462555\webapp`.
- Stack traces: `preview_logs` serverId b56b2458, level=error, search=keyword.
- Verify trio (webapp/): `npx tsc --noEmit` · `npx vitest run` (670) · `npm run replay-pilot` (30).
- E02 `8b32b3e0-…`. Animatic v6 `6c185bac` (29 shots/70s; SH03/SH04 override→3s). Cost math: standard $0.3024/s × dur × resmult (720p=1, 1080p=2.25); 4s×1080p=$2.722, 4s×720p=$1.21.
