// Флаг in-process режима инструментов (Ф4.1). ОБЯЗАН исполниться ДО импорта
// любого модуля из `scripts/run/*` — иначе `defineTool` пойдёт CLI-путём: начнёт
// парсить argv процесса приложения и завершит его. Порядок гарантирован ESM:
// в `tool-bridge.ts` этот модуль стоит ПЕРВЫМ импортом, а модули исполняются в
// порядке объявления, в глубину. Отдельный файл — именно ради этой гарантии:
// присваивание в теле самого моста исполнилось бы ПОСЛЕ его импортов.
process.env.SS_TOOLS_HARNESS = '1';

export {};
