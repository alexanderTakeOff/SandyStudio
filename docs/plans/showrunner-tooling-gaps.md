# Журнал разрывов: шоураннер канала ↔ Полина

> **Живой документ. Заводится 2026-07-27 в сессии обкатки канала PRAGMATIC.**
> Директор: «всё, что мы сейчас делаем, рассматривай через призму — умеет ли это же
> самое Полина. Задача не создать манифест прагматика, а обкатать инструмент и понять,
> что прибито гвоздями к песочным часам. Код сейчас не правим — только заметки и план.»
>
> Метод: шоураннер (Claude Code, полный доступ) выполняет реальную работу канала.
> Каждый шаг сверяется с тем, дотянулась бы до него Полина (webapp, `lib/concierge/tools/*`).
> Разрыв → строка в этом журнале. Статистика набирается по мере эпизодов, не за один раз.

**Легенда:** 🔴 блокирует автономность · 🟡 обходится вручную · 🟢 закрыто

---

## A. Прибито гвоздями к песочным часам

Наследие одного сериала, вылезшее при втором. Все пункты подтверждены чтением кода.

### A1 🔴 Тема = «visual gag engine» в словаре инструмента Полины

`webapp/lib/concierge/tools/themes.ts` — оба описания тула определяют тему как
«reusable visual gag engine», а `proposeTheme` требует «mechanism, escalation, why it
fits the series». Для SS-S17 тема — это **тезис-карточка** (Bible §34 Stage 2: тезис ·
личная призма · границы применимости · практическое следствие · хук · источники · риск
банальности). Полина, читая это описание на Pragmatico, будет авторить комедийные
движки против библии канала.

**План:** описание тула сделать нейтральным — «episode engine, форму которого объявляет
Библия серии», а конкретную форму подмешивать из Библии в системный промпт (там уже
есть per-series контур, `system-prompt-builder`). НЕ плодить второй тул для эссе.

### A2 🟡 Тот же протёк в слое хранения

`webapp/lib/api/series-themes.ts`, шапка файла: «A theme is a reusable visual gag engine
for an episode». Комментарий, но именно из него следующий агент выведет форму.
**План:** переписать шапку в терминах роли, не жанра.

### A3 🔴 Пара скилл-тем построена на gag engine

`.claude/skills/series-episode-theme-generation` + `-selection`: объект — gag engine,
гейты отбора включают «gag-engine capacity», «silent readability», «character physical
fit». Для голосового эссе-канала без героя это чужая форма — шоураннер PRAGMATIC не
смог ими воспользоваться и собрал портфель руками.
**План:** уже висит отдельной задачей (`task_69938c0e`): абстрагировать пару, объект →
«движок эпизода», гейты разделить на универсальные и объявленные Библией. Anti-additivity:
абстракция существующей пары, НЕ второй скилл для эссе.

### A4 🟡 Дефолт формата — landscape

`webapp/lib/agents/delivery-targets.ts:53` — фолбэк `['youtube_landscape']`. Для
shorts-first канала нейтральный дефолт оказывается ландшафтным (наследие Sandy-эпохи).
Сейчас обойдено: `series.metadata.delivery_targets = ['youtube_shorts']` у SS-S17.
**План:** решить, остаётся ли ландшафт студийным дефолтом или дефолт переезжает в
паспорт канала. Не менять молча — у Sandy есть long-form.

### A5 🟡 Категория заливки по умолчанию — Comedy

`specs/distribution/youtube.md` §43: фолбэк `categoryId '23'` (Comedy). Для эссе-канала
неверно; обойдено `publish_defaults.category_id = '22'` (People & Blogs) в паспорте.
**План:** снять жанровый фолбэк на уровень паспорта, в коде оставить «нет значения →
не отправлять поле».

---

## B. Чего Полина не умеет из того, что делал шоураннер

Сверено с реестром `webapp/lib/concierge/tools/index.ts` (2026-07-27).

| # | Что делал шоураннер | Полина | Что нужно |
|---|---|---|---|
| B1 🔴 | Заполнил `channels.metadata.branding` (оверлей, описание, теги, CTA, boilerplate) | нет тула, нет даже `listChannels` | read+write тул паспорта канала поверх существующего `PATCH /api/channels/[id]` (там уже zod-схема и мерж секций) |
| B2 🔴 | Проставил `publish_defaults` (категория, язык, made-for-kids) | нет | тем же тулом, та же ручка |
| B3 🟡 | Проставил `series.metadata.delivery_targets` | нет | тул формата серии либо поле в существующем series-туле |
| B4 🔴 | Читал ратифицированные уроки канала (`hog_memory`) и писал решения | нет ни чтения, ни записи | read-тул уроков + write-тул решений; писать журнал ТОЛЬКО kind decision/hypothesis (kinds `daily_advice`/rollup остаются за кроном, как в `scripts/hog-memory-append.mts`) |
| B5 🔴 | Смотрел запас эфира (снапшоты `docs/distribution/snapshots/*`) | нет | тул «runway»: сколько дней оплаченного эфира осталось; сторожок в HoG-daily уже есть, Полина его не видит |
| B6 🟡 | Записал описание, keywords и баннер на страницу канала YouTube | нет | тул поверх `channels.update` + `channelBanners.insert`. Аватар API не умеет вообще — только руками |
| B7 🟡 | Сгенерировал баннер и аватар (ffmpeg-типографика) | нет | либо тул брендинг-ассетов, либо явно оставить Директору |
| B8 🟡 | Читал внешние источники (Gemini Notebook, веб) | нет браузера | признать human-in-loop: сырьё приносит Директор. Публичного API у Gemini Notebook нет (только Enterprise) |
| B9 🟢 | Правил Библию серии (§17, язык публикации) | **умеет** — `setBibleContent` | — |
| B10 🟢 | Писал темы в per-series поверхность | **умеет** — `listThemes` / `proposeTheme` | но см. A1 (словарь) и C2 (по одной за вызов, портфель не выражается) |

### B11 🔴 Нет ни голоса, ни субтитров, ни AI-раскрытия — Pragmatico не производим автономно

Найдено 2026-07-27 при разборе вопроса «есть ли смысл в эссе-канале». Три отсутствующие
возможности, все подтверждены чтением кода:

1. **Голосового провайдера нет.** ElevenLabs зарегистрирован в
   `lib/agents/provider-resolver.ts:66` и `lib/api/provider-catalog.ts:181` только как
   `elevenlabs-sfx` (звуковые эффекты). TTS/озвучки нет. В ростере `agents/exec/` 23 роли
   — ни диктора, ни редактора звука речи. Библия SS-S17 §31 при этом объявляет голос
   смысловым центром, а §40 сама признаёт: «до появления автоматического VoiceProvider /
   TTS допускается заранее подготовленная утверждённая голосовая дорожка».
2. **Тайм-кодированных субтитров нет.** `lib/agents/providers/ffmpeg-shorts.ts` умеет
   вжечь ОДНУ статичную подпись на первые N секунд плюс хвостовой CTA
   (`buildShortFilter`). Библия §30 требует обязательные субтитры, точно совпадающие с
   финальной голосовой дорожкой. Это другая возможность, а не настройка существующей.
3. **AI-раскрытия в загрузчике нет.** `lib/agents/providers/youtube.ts` выставляет только
   `status.selfDeclaredMadeForKids` (строка 269). Поля «altered or synthetic content» нет.

**Почему это важнее, чем звучит.** Для Sandy пункт 3 не нужен: политика YouTube не требует
раскрытия для нереалистичного/анимационного AI, а стилизация — сама по себе щит. Библия
Pragmatico §24 требует «узнаваемую современную реальность» и кинематографичность, то есть
фотореалистичную зону, где раскрытие может стать обязательным, а с мая 2026 работает
автоматическая маркировка.

**Следствие для планеты.** Пока голос ручной, эссе-канал по определению не проходит
критерий текущей планеты «незапланированных ручных касаний ноль» — самый смыслонесущий
слой делается руками Директора.

**План:** решение Директора ДО постановки в очередь — (а) строим станцию голоса + субтитры
+ поле раскрытия, тогда второй жанр становится честным тестом переносимости; (б) канал
живёт как ручной авторский проект вне фабрики; (в) жанр меняется на такой, где голос не
нужен. Кодеру до решения делать нечего.

---

## C. Структурные дыры (модель данных, не инструмент)

### C1 🔴 У сырья (Thought Capture) нет дома в БД

Bible §34 Stage 1–2 требует, чтобы тезис жил в системе от заметки до экспорта. Сырьё
Александра легло в `FILMS/Pragmatico/thought_bank.md` — Tier-2, локально, не в git,
Полина туда не достанет. Значит Stage 1 у эссе-канала вне системы.
**План:** дать сырью тот же приём, что темам — свой `file_type` (например
`SPC-thought_{slug}`, series-scoped, вне `SBL-%` и вне канон-гейтов, ровно по образцу
`series-themes.ts`). Ноль миграций, если префикс уже разрешён CHECK-констрейнтом; иначе
одна миграция. Переиспользовать поверхность тем, а не строить вторую.

### C2 🔴 Портфеля как объекта не существует

Порядок слотов, тестируемая переменная, класс стоимости, баланс explore/exploit живут
только текстом внутри `content`. Запросить «покажи портфель по порядку» нечем; шоураннер
кодирует это в заголовке «Слот 1 …», то есть соглашением, а не полем.
**План:** добавить в `metadata` темы несколько полей (`portfolio_slot`,
`tested_variable`, `cost_class`, `stance: explore|exploit`) — metadata уже свободная
(`theme_status` там же живёт), миграция не нужна. Тогда портфель = сортировка списка.

### C3 🟡 Авторство темы искажается

`POST /api/series/[id]/themes` требует Director-сессию и штампует `agent_id: 'Director'`
плюс `created_by_kind: 'director'` из `requireDirector()`. Тема, предложенная Полиной,
выглядит созданной Директором → завышает его касания в scorecard. Совпадает с известным
`backlog_scorecard_polina_mislabel`.
**План:** прокидывать фактического автора (Полина / шоураннер / Директор) в провенанс.
Шоураннер в этой сессии писал напрямую в БД и проставил
`created_by: 'showrunner:PRAGMATIC (Тео)'`, `created_by_kind: 'agent'` — то есть модель
данных это УЖЕ умеет, дыра только в роуте.

### C4 🟡 Журнал канала пишется только из CLI

`hog_memory` пополняется скриптом `webapp/scripts/hog-memory-append.mts` из headless-CLI.
Вебапп в журнал не пишет — значит решения, принятые в чате с Полиной, в память канала
не попадают.
**План:** тонкая ручка поверх той же таблицы с тем же ограничением по `kind` (см. B4).

---

## D. Что сработало как задумано

Фиксируем, чтобы не переделывать здоровое.

- **Каскад брендинга** `series → channel → нейтрал` (`lib/agents/branding.ts`) — второй
  канал завёлся без единой строки кода.
- **`publish_defaults` per-channel** (Фаза 4e) — тоже данные, не код.
- **Поверхность тем per-series** как ассеты `SPC-theme_{slug}` — вне канон-гейтов,
  без миграций, статус курации в metadata. Второй сериал лёг в неё как родной.
- **`delivery_targets` как единый сигнал формата** — один ключ развернул Storyboarder
  в вертикально-безопасный кадр, EREF в 1024×1536, Animator в 9:16. Проверено прогоном
  резолверов на живых строках.
- **Разделение «студия ↔ канал ↔ серия»** — паспорт канала, серия, Библия серии не
  протекли друг в друга.

---

## E. Порядок работ для кодера (черновик, приоритет сверху)

1. **A1 + A2** — расжанрить словарь тем (описание тула + шапка слоя). Дешевле всего,
   а без этого Полина на Pragmatico будет авторить не то.
2. **B1 + B2** — тул паспорта канала поверх готового PATCH-роута. Открывает Полине
   всю кастомизацию канала.
3. **C2** — поля портфеля в `metadata` темы. Без миграции, превращает список тем в
   портфель.
4. **B4 + C4** — чтение уроков и запись решений в журнал канала. Без этого шоураннер
   в вебаппе слеп и беспамятен.
5. **C1** — дом для сырья (`SPC-thought_*`), по образцу тем.
6. **C3** — честное авторство в роуте тем.
7. **B5** — тул запаса эфира.
8. **A4 + A5** — снять жанровые дефолты в паспорт (требует решения Директора, у Sandy
   есть long-form).
9. **A3** — абстракция скилл-пары тем (уже отдельной задачей).
10. **B6 + B7** — брендинг канала и генерация ассетов. Ниже всех: делается один раз на
    канал, ручной обход дешёвый.

---

## F. Статистика обкатки

| Дата | Канал | Что обкатывали | Новых разрывов | Закрыто |
|---|---|---|---|---|
| 2026-07-27 | PRAGMATIC | кастомизация канала + первый портфель тем | 15 (A1–A5, B1–B8, C1–C4) | 2 подтверждено рабочими (B9, B10) |

---

## G. Разрывы завода СЕРИАЛА (не эпизода) — обкатка «ГЛУБИНЫ», 2026-07-30

Прежние разрывы собраны на кастомизации канала и портфеле тем. Здесь другая обкатка: завод
нового сериала с нуля — мир, канон, первые ассеты Библии. Директор: навыки Полины создавались
и тестировались **только на эпизодах**, поэтому для завода сериала их фактически нет.

| ID | Разрыв | Доказательство | Цена обхода |
|---|---|---|---|
| **D1** | **Полина не может прикрепить к ассету УЖЕ утверждённый кадр с диска.** Её ответ дословно: «у меня нет прямого инструмента для загрузки файла по пути на диске… Мне не хватает инструмента, который мог бы напрямую загрузить файл с диска и прикрепить его к ассету Библии без перегенерации». | Тред `912d7a04`, 11:37 UTC. Перепроверено: `copyAssetImage` требует ассет-ИСТОЧНИК с картинкой, а утверждённые кадры ассетами не являются — чтобы ими стать, нужна та же отсутствующая способность. Круг замкнут. `regenerateBibleImage` не подходит: заплатим ещё раз и получим ДРУГОЙ кадр. | Способность в студии есть, но не у неё: маршрут `POST /api/assets/{id}/upload` (Директор в UI) либо санкционированный скрипт `replace-locked-canon-image.ts`. Обход дешёвый, но ручной. |
| **D2** | **Отклонённый вызов отравляет сторожок дубликатов.** `auto-react-loop.ts:evaluateRound` пишет подписи в `seen` для ВСЕХ вызовов раунда независимо от результата, поэтому вызов, отклонённый проверкой одобрения, считается сделанным, и повтор — уже с одобрением — падает как `duplicate mutating call`. | Тред `912d7a04`: 11:30:59 `verbal_approval_required` → 11:31:07 `duplicate mutating call setBibleContent`. Ассет при этом НЕ создан (проверено по `assets`). | Новое сообщение = новый запрос с чистым `seen`. Внутри одного запроса исправиться она не может вообще. |
| **D3** | **Канон не имеет состояния нигде** — ни в матрице (`STAGE_ORDER` начинается с `ref_plan`), ни в DAG (`EXEC-BIBLE-AUTHOR`: `required: []`, «no Inngest function — gate is informational only»). | `state-matrix.ts:31`, `gate.ts:261`. | Порядок держится текстом в `canon_minimum.md`, который код не видит. Следствие: у стоячего одобрения вне эпизода нет объекта, к которому прикрепиться → в Mode 2 канон сериала это круги подтверждений. |
| **D4** | **Порядок завода сериала, который предлагает сама Полина, не содержит визуального канона** — от общей идеи прямо к сценаристу. | Тред `912d7a04` и предыдущий, 30.07. | Это и есть назначение скилла `ss-series-starter`: вставить блок, которого в её модели мира нет вообще. |

**Приоритет:** D2 дешевле всех и бьёт по любому сценарию (одна строка: записывать подпись только
для исполненных вызовов). D1 — второй, потому что завод любого сериала начинается с утверждённых
снаружи кадров. D3 — большой и проектируется из завершённого прогона, а не из догадки. D4 закрывается
скиллом, без кода.

Разбор и порядок: `docs/plans/series-bootstrap-run-notes.md` (§1 порядок · §6 состояние · §7 поломки).

**D5 (найден 15:55, структурный) — Полина не может ЗАКРЫТЬ канон.** Инструмента замка у неё
нет вовсе (в 48 тулах `lockAsset` отсутствует), а маршрут
`POST /api/series/{id}/bible/{assetId}/lock` вызывает `assertHumanDirector` — служебному токену
EXEC-DIR-AI отказано в любом режиме. При этом канон-разрыв считает **только** `status='LOCKED'`
(`series-bible.ts:302,396`), то есть черновики он не видит.

Следствие важнее самого разрыва: **завод сериала через Полину в принципе не может дойти до
состояния, которое требует префлайт.** Она способна собрать весь канон и упереться в дверь, у
которой у неё нет ручки. Это правильное управление (LOCK — хард-лимит Директора по CLAUDE.md §6),
но в скилле `ss-series-starter` оно обязано стоять как ЗАПЛАНИРОВАННАЯ конечная точка блока, а
не как внезапная остановка: «канон собран, дальше Директор закрывает замки в UI».

`approveAsset` замок не заменяет: он ставит `APPROVED`, и в `HARD_LIMIT_TOOLS` его нет —
то есть APPROVED Полина поставить может, а LOCKED никогда.

**D6 (найден 16:15) — гейт `EXEC-EREF` требует LOCKED ассет секции `character`, и это НЕ дефект.**
`gate.ts:594` жёстко требует `counts.character >= 1` И `counts.style >= 1`. У «Глубины» после
замка первых двух ассетов: `style=1` ✓, `object=1`, **`character=0`** ✗ — лица героя в этом мире
нет по замыслу, герой это взгляд, а аппарат канонизирован как `object`.

Первым порывом было либо переложить аппарат в `character`, либо править гейт. Оба хода лишние:
канон-минимум §8 уже содержит `SBL-character_tooth_leviathan` — существо первого эпизода. Оно и
есть character, и оно же канарейка (§«Порядок исполнения» п.2 — «грань зуба в луче на утверждённом
стиле»). То есть порядок был верен изначально, а гейт закроется сам следующим шагом.

**Что из этого идёт в скилл** (и это единственное, что тут ценно): требование «сериалу нужен
хотя бы один LOCKED character» **невидимо до запуска EXEC-EREF**. Для сериала, чей герой не
человек и не лицо, это выглядит как внезапный отказ на поздней стадии. Скилл обязан проверять
это в НАЧАЛЕ канон-блока — «кто здесь character?» — а не узнавать на префлайте.

**D7 (найден 16:40, САМЫЙ ВАЖНЫЙ за день) — канон Библии перегенерируется БЕЗ референсов.**
Моя же запись в досье была неверна и исправлена. Факт: в
`app/api/assets/[id]/regenerate-image/route.ts` мульти-референсная ветка открывается условием
`if (isV2 && v2Sr)`, где `v2Sr` — `test_plan` v2-ассета КАДРА. У ассета Библии (`SBL-*`)
`test_plan` не существует, поэтому вызов падает в legacy-ветку `generateImageOpenAI`: чистый
text-to-image, **ноль референсов**, `1024x1024`/section-aware размер.

**Следствие:** каждый канон-ассет серии, начиная со второго, рисуется с чистого листа и не
видит ни стиль-якоря, ни героя. Это ровно та причина расхождения канона Sandy, которую Директор
опознал 30.07 — и она ЖИВА, а не исправлена. Инструмент Полины `regenerateBibleImage` ведёт в
этот же маршрут, то есть **через Полину анкоренный канон получить сегодня невозможно**.

Перепроверено дважды, как велено. Тело запроса маршрута принимает `style_anchor_asset_id`
(строка 88 схемы), но для не-v2 оно только пишется в метаданные (строка ~788) и в сборку
референсов НЕ попадает — референсы читаются исключительно из `v2Sr.test_plan`. Иного пути у неё нет.

Рабочий мульти-реф на уровне Библии существует ровно один и он скриптовый:
`scripts/gen-brand-visual.ts` — `openAIEditsMultiProvider` + `readBibleImageAsBase64` по LOCKED
канону. Он и доказывает, что все части на месте: не хватает только ветки в маршруте.

**Приоритет: выше D2.** D2 мешает Полине исправиться, D7 портит сам продукт — канон, на который
потом ссылается каждый кадр эпизода.

---

**D8 (найден 30.07 вечером, закрыт в тот же вечер) — писатель канона не умел ПРАВКУ.**
Указание Директора: «Полина должна вызывать писателя канона с нашими правками». Оказалось,
нечем. `enrichBible` принимал единственный `assetId`; тело HTTP-запроса — только
`{ directorConfirm }`; затравка бралась из колонки `description`, а не из `content`; писатель
свой же текст не читал и в конце затирал и `content`, и `description`. Он умел ровно одно —
сочинить с нуля. Значит любую правку канона физически выполнял человек, переписывая статью
целиком: тот же дефект «администратор, не автор», за который вечером ловили Полину, только
уехавший на уровень выше — в меня.

**Закрыто** (`802dcc10`): у `enrichBible` появился необязательный `notes`. Он же переключатель
режима — текущая статья уходит писателю неприкосновенной БАЗОЙ, правки ложатся дельтой поверх
и последними в промпте. Гейт по `image_prompt.history` остался против СЛУЧАЙНОГО повтора и снят
для явной ревизии; LOCKED отказывает всегда. Потолок вывода на ревизии 2000 → 4000, иначе
статья обрубается на середине воспроизведения базы.

**Доказано рантаймом дважды:** интерьер (18:46, $0.1161) и гравировка (19:04, $0.1095). Оба
раза Полина вызвала инструмент **с первого сообщения**, без зависания на `getAsset` — то самое,
на чём она вставала трижды за вечер. Диагноз подтверждён: вставало не «редактирование», а
отсутствие инструмента под него.

**D9 (найден 30.07, закрыт) — нельзя было рисовать ПОВЕРХ своего же кадра.**
Директор про интерьер: «эту картинку можно взять за образец, убрать лишнее и сдвинуть джойстик,
компоновку оставить». Сборщик запрещал это дважды: `excludeAssetId` («never reference the asset
being generated») и фильтр `status in (LOCKED, APPROVED)` — а интерьер был DRAFT. Единственным
способом «поправить» кадр была перерисовка с нуля, то есть потеря компоновки.

**Закрыто** тем же коммитом и без новой сущности: запись может назвать **саму себя** на строке
`Refs:`, и только тогда для этой одной строки снимаются и исключение, и фильтр статуса.
Умолчание не изменилось. Рантайм: `references_used = [location d14bb17b (сам интерьер),
identity 7a6b2452, style 10916888]` — база поехала первой.

Общий вывод обоих: **композиция переживает правку, только если приходит пикселями.** Словами
она каждый раз выводится заново, и «оставь как было» словами невыполнимо в принципе.

**D10 🟡 (найден 30.07, открыт) — ревизия ЗАТИРАЕТ историю картинок.**
`runBibleAuthor` пишет свежий `image_prompt`, поэтому после правки `history` снова длиной 1, а
прежние версии промптов исчезают. Для первой генерации это верно, для ревизии — потеря: нельзя
сравнить, что именно изменилось между попытками, а это главный инструмент разбора «почему стало
хуже». Чинится дописыванием в существующий массив вместо замены.

**D11 🟡 (найден 30.07, открыт) — два ассета секции `style` конкурируют за роль якоря.**
Сборщик выбирает стиль-якорь как ПЕРВЫЙ подходящий `SBL-style_*` в порядке «LOCKED раньше
APPROVED, затем по дате создания». Пока типографика гравировки (`SBL-style_engraving_type`)
моложе настоящего якоря, всё работает случайно-верно. Стоит появиться второй записи секции
`style`, залоченной раньше, — и якорем станет она. Роль якоря обязана быть объявленной, а не
выводиться из сортировки.

**D12 🟡 (найден 30.07, закрыт на уровне роли) — писатель не знал закона плиты.**
Дважды за вечер, с интервалом в час, писатель внёс в канон-плиту то, что решает ЭПИЗОД:
интерьер получил освещённую воду за стеклом, типографика — врезанную фразу «WE WENT NO DEEPER».
Обе плиты прикладываются референсом к каждому будущему кадру, поэтому каждая утащила бы свою
случайность во все кадры сериала разом. Дефект не в кадре и не в модели — закона плиты в
инструкции писателя не было вообще.

**Закрыто** разделом «The plate law» в `agents/exec/bible_author.md`: таблица «что держит плита
· что решает эпизод» и проверка перед RENDER — «была бы эта деталь верна в КАЖДОМ эпизоде?».
Заодно всплыло подтверждение из ТЗ пилота: п.5 требует две языковые версии без перегенерации
кадра, то есть фраза обязана быть оверлеем, и врезанная в плиту фраза ломала бы приёмку.

**D10 🟢 ЗАКРЫТ 31.07** (`6732ccb3`). `nextVersionFor` в `lib/api/series-bible.ts` — одно
правило нумерации версий на всех писателей, вынесено из `regenerate-image` (где оно уже
обожглось на E30 SH44) и позвано из `bible-author`. Ревизия теперь ДОПИСЫВАЕТ и
`image_prompt`, и `description_history`, а не пересобирает их с `version: 1`. Рантайм-проверка
на зубе: номер продолжился с **3**, а не сбросился в 1.

**D11 🟢 ЗАКРЫТ 31.07 — но НЕ тем способом, который я предлагал.**
Моё предложение было «короновать якорь сортировкой». Директор его отклонил вопросом, который
оказался точнее диагноза: «причём здесь старшая или младшая? Стиль это стиль, а reference это
reference. Для видеогенератора стиль идёт как-то по-особенному?»

Ответ: **нет, не по-особенному.** Для провайдера это обычная референс-картинка, `kind` —
ярлык. Особенным его делает наш собственный сборщик промптов, и там словом `style` названы
ДВЕ разные вещи:
1. **документ художественного направления серии** — его ТЕКСТ подставляется писателю как
   «Series art direction (must follow this)». Такой обязан быть ровно один на серию;
2. **референсная плита** — картинка, которую прикладывают. Их сколько угодно.

Гравировка была вторым с именем первого. Значит дефект — в ИМЕНОВАНИИ, а не в сортировке, и
лечится вычитанием: запись перенесена в `object` (Директор, q6), в секции `style` снова одна
запись, `bible.styles[0]` перестал быть лотереей сам собой. Хелпер не понадобился.

Замер по базе, ради которого стоило смотреть: **S14 несёт ЕЛЕВЕН записей секции `style`, из
них ТРИ LOCKED** (`..._location_plate_h`, `..._object_plate_j`, `s14_canon_v1`) — то есть
монетка живёт с 12 мая, просто на легаси-сериале её никто не ловил. S14 не чиним: §6
конституции, легаси обратной совместимости не получает.

Отдельно закрыт латентный корень: `loadBibleCanon` в `episode-references.ts` и три
её сестры (`bible-loader`, `continuity-check`, `thumbnail-renderer`) тянули канон **без
`ORDER BY`** и отдавали `styles[0]`. Порядок добавлен во все четыре — запрос без порядка
вреден сам по себе, независимо от того, сколько записей в секции сегодня.

**D13 🟡 (найден 31.07, открыт) — `upload` недоступен headless-агенту.**
`POST /api/assets/{id}/upload` требует человеческую сессию и отвечает 307 на `/login`. Значит
путь «файл с диска как реф», о котором Директор спрашивал, для агента закрыт: пришлось идти
скриптом `replace-locked-canon-image.ts` (он делает `persistBinary` в Drive и сверяет md5).
Для Полины путь закрыт тем более — у неё вообще нет инструмента приложить готовый файл.

**D14 🔴 (найден 31.07, открыт) — преflight каста не принимает форму слага, которую сам же показывает.**
`validateCanonExists` (`lib/api/series-bible.ts:553-557`) строит множество «что есть» через
`bibleSlug(file_type)`, то есть в КОРОТКОЙ форме без секции: `SBL-character_bathyscaphe_turnaround`
→ `bathyscaphe_turnaround`. А `listSeriesBibles` и дайджест канона показывают Полине форму С
секцией — поэтому она естественно передаёт `character_bathyscaphe_turnaround`, совпадения нет, и
маршрут честно отвечает «нет LOCKED канона» на канон, который залочен.

Это ТОТ ЖЕ дефект, что 30.07 ловился на строке `Refs:` у писателя канона и был там закрыт
функцией `matchesSlug` (`lib/agents/series-canon-refs.ts`), принимающей ОБЕ формы. Причина
одинаковая: поверхности, которые ПОКАЗЫВАЮТ канон, называют его секционно, а сверяющие функции
ждут короткую форму. Лечится переносом того же `matchesSlug` в `validateCanonExists` — одно
место, обе формы.

**D15 🔴 (найден 31.07, ЗАКРЫТ обходом) — запись в ЗАКРЫТЫЙ тред отвечает `success`.**
Сторож простоя (`inngest/functions/pa-batch-stall-watchdog.ts:74`, TTL 180 мин) закрыл тред
прогона в 03:05. После этого `POST /api/team-chat/post` с явным `--thread` проверял только
СУЩЕСТВОВАНИЕ треда (`route.ts:105-113`), а не `ended_at` — три сообщения легли в мёртвый тред с
рапортом об успехе, событие ушло, `exec-pa-react` отработал и вернул `skipped: thread_ended`.
Полдня выглядело как «серверное пробуждение сломано»; на деле сломан был адрес.

Обход: не передавать `--thread` — маршрут сам возьмёт последний ОТКРЫТЫЙ. Заодно чинится
раскол: Директор в браузерном треде, я в мёртвом — его ответов он бы и не увидел.

Три однострочные правки: (1) добавить `.is('ended_at', null)` в ветку явного треда, чтобы запись
падала громко; (2) `exec-pa-react.ts:39-42` слепо верит переданному id — должен проверять, открыт
ли тред, и тогда сбой самозалечивается; (3) `.catch` отправки события молчит в production
(`post/route.ts:142-147`) — снять условие `NODE_ENV`.

**D16 🟡 (найден 31.07, открыт) — автор брифа слеп по конструкции, и потолок ввода тут ни при чём.**
Расширять `premise` бессмысленно: промпт автора брифа велит «clarify, don't extend», держать
600 слов, лимит 1500 токенов (`lib/agents/providers/anthropic-brief.ts:36,71,89,90`). Глава
текста будет сжата в логлайн. Themes задачу тоже не закрывают: пайплайн темы НЕ читает нигде
(намеренно — они не `SBL-`, чтобы быть невидимыми канон-гейту), а `listThemes` не отдаёт ни id,
ни содержимое.

Нужный сосуд уже есть — `SPC-start_notice` (200 000 символов, инструмент записи, назначение
дословно «до-авторская информация от Директора к Сценаристу»). Два дефекта: он невидим человеку
(нет ветки в `STAGE_FROM_ASSET`) и читается на стадию позже, чем нужно — только сценаристом
(`screenwriter.ts:319`), НЕ автором брифа и НЕ раскадровщиком.

Правка целиком из переиспользования: автор брифа читает заметку + канон серии (загрузчик
`loadSeriesBibleCanon` уже есть), контракт вывода ослабляется, в модалку создания эпизода
добавляется поле «исходный материал», а читаемость даёт связка существующих `StageKebabMenu` +
`EditorModal`. Блокер: заметка пишется в статусе APPROVED, а редактор пускает только
DRAFT/REVIEW/REVISION — и понизить статус нельзя, иначе сценарист её не увидит.

**D17 🟡 (найден 31.07, открыт) — выбор модели есть у двух точек из восемнадцати.**
Управляемы Полина и визуальный критик; остальное — константы в раннерах (раскадровщик на Opus,
сценарист, дизайнер референсов, аниматор по шоту, семь критиков, автор брифа). `resolveModelId`
как рычаг мёртв: его зовёт ОДИН раннер, поле `model:` в реестре помечено описательным и уже
разъехалось (копирайтер: реестр `haiku`, код Sonnet).

Сведение вычитающее и без миграции: `ModelPickerRow` уже универсальный, конфиг ложится в
существующий scope `providers` ключами `llm:<slot>`. Две ловушки обязаны войти в ту же правку:
(1) `MODEL_RATES` не знает новых моделей и молча считает их по ставке Sonnet — леджер станет
фикцией; (2) зрение ходит только в Anthropic, не-`claude` значение даст 404, а детектор on-model
уже принимает произвольную строку из конфига.

**D18 🟡 (Директор, 31.07) — у Полины темы только пишутся, но не читаются и не переключаются.**
Есть `proposeTheme` и `listThemes`, но `listThemes` возвращает только слаг, описание, статус и
`used_in_episodes` — **ни id, ни содержимого** (`lib/concierge/tools/themes.ts:189-194`). Значит
цепочка `listThemes → getAsset(id)` невозможна: она умеет тему записать и не умеет прочитать
обратно. Инструмента смены статуса (`approved/draft/used/invalidated`) у неё нет вовсе — статус
сегодня переключается только руками в интерфейсе.

Директор: **нужен инструмент чтения темы и инструмент смены её статуса.** Обе правки идут в
существующий `themes.ts`, новых сущностей не требуется: отдавать `id` (и опционально `content`)
в `listThemes` плюс `setThemeStatus`. Смежное: системный промпт всё ещё направляет вопросы про
темы в «Seed Bank» Библии, а не во вкладку Themes (`lib/concierge/system-prompt-builder.ts:220`)
— две конкурирующие поверхности для одной сущности.

---

## Разрывы дня 31.07 (после первых референсов SS-S20-E01)

**D19 ✅ (найден и закрыт 31.07) — инструкция роли кэшируется на весь процесс.**
`episode-reference-designer.ts:272` держал `systemPromptCache` до конца жизни процесса, и
правка `agents/exec/*.md` не доходила до работающего сервера. Отказ **молчаливый**: агент
авторит по прошлой редакции и ничего об этом не сообщает. Стоило круга — Дизайнер дважды
перезапустили против исправленной инструкции, и дважды он взял старую. Два датированных
блока комментариев над кэшем существовали ровно как «касания», чтобы HMR перезагрузил модуль.
Закрыто: кэш проверяет mtime файла. Смежное: **тем же способом кэшируют все девять runner'ов**
(`animator`, `storyboarder`, `script-reviewer`, `copywriter`, `continuity-check`,
`thumbnail-designer`, `episode-reference-critic`, `creative-readability-critic`) — там ловушка
осталась. Скиллы этой болезнью не болеют: у селектора TTL 30 секунд.

**D20 ✅ (найден и закрыт 31.07) — Дизайнер референсов не грузил скиллы вообще.**
Единственный из авторов: сценарист, раскадровщик и критик читаемости грузят, он — нет. Поэтому
любое жанровое правило могло жить только в его файле роли, и правило комедии (ACTING-FIRST,
«вести промпт лицом») применялось к сериалу, у которого лица нет. Закрыто самым дешёвым
путём — `thumbnail-designer`: тела скиллов напрямую, без манифеста и без платного отбора.

**D21 ✅ (найден и закрыт 31.07) — `eref-designer` был мёртв с рождения.**
Фронтматтер объявлял `applies_when.agent: [EXEC-EREF]`, а Дизайнер — это `EXEC-EREF-DESIGNER`
(`EXEC-EREF` — другой агент, Художник). Совпадение строк точное, значит playbook не
подхватывался НИ РАЗУ с 18.05. Тот же класс, что молча мёртвая `sandy-gag-library`.

**D22 🔴 (найден 31.07, открыт) — комментарий во фронтматтере роняет скилл целиком.**
Самодельный парсер (`lib/skills/load-skill-file.ts`) не понимает `#`-строки внутри
`applies_when` и отбрасывает **весь файл**: `DROPPED "eref-designer" — Malformed nested line`.
То есть попытка объяснить в скилле, почему у него такой скоуп, убивает скилл. Обход: комментарии
только в теле. Правка: либо парсер пропускает комментарии, либо запрет пишется в шаблон скилла.

**D23 🔴 (найден 31.07, открыт) — «слепой PASS» встроен by design.**
`script-reviewer.ts:216,222` даёт критику ТОЛЬКО бриф и сценарий. Своего прошлого вердикта он не
видит, значит физически не может проверить, устранены ли его же замечания — и не проверил:
единственное верное замечание v01 («warm teal-green» против канона) уцелело в v02, а критик
снова дал PASS. Вторая половина той же дыры: `runner.ts:307` грузит записки из `approvals` с
фильтром `approval_type='APPROVE'`, поэтому записка, написанная при отправке **на доработку** —
ровно та, где сказано, что не так — не доезжает ни до одного агента. У сценариста есть обходной
путь (`revisionNote` в теле события), у критика нет никакого.
Правка тройная: снять фильтр (одна удалённая строка чинит записки для ВСЕХ агентов) · критик
читает прошлый `REV-script_qa` из уже загруженного набора · **не устранённое замечание прошлого
круга запрещает вердикт PASS**.

**D24 ✅ (найден и закрыт 31.07) — критик читаемости HALT'ил на каждой версии раскадровки.**
Единственный движок читаемости написан под комедию (`genre: [comedy]`), у серии стоял жанр
`other`, совпадения нет → `hard`-движок не найден → HALT без вызова модели. Это КОРРЕКТНОЕ
поведение, а не поломка: критик честно отказывается судить жанр, для которого у него нет
грамматики. Закрыто заведением жанра `cosmic_horror` + движка `readability-cosmic-horror`.
Урок для `ss-series-starter`: **заведение сериала обязано включать шаг «есть ли движок критика
под этот жанр»**, иначе конвейер встанет на раскадровке и это выяснится только там.

**D25 🔴 (найден 31.07, открыт) — потолок 2000 символов на записку о доработке.**
`requestRevision.note` ограничен 2000 (`lib/concierge/tools/dispatch.ts`), а содержательная
правка раскадровки из семи пунктов — вчетверо длиннее. Полина получила «Request body failed
validation» и предложила сократить замечания. Обход найден без кода: полный текст в
Episode Start Notice (потолок 200 000, раскадровщик читает его штатно), в записке — ссылка.
Правка: поднять потолок либо штатно поддержать эту связку.

**D26 ✅ (найден и закрыт 31.07) — канон-минимум держал две плиты одного объекта.**
`tooth_leviathan` и `leviathan_pyramid_tooth` описывали ОДНО И ТО ЖЕ в одном масштабе: текст
первого открывался словами «Зуб это ГРАНЁНОЕ тело, близкое к пирамиде». Плиты, которую требует
первая половина эпизода — плоская грань во весь кадр, без вершины, основания и силуэта, — не
существовало. Раскадровщику было не из чего выбрать, и он поставил на кадр «носом в поверхность»
канон пирамиды; каждый рендер выгибал плоскую грань обратно в пирамиду. Закрыто переопределением
`tooth_leviathan` в плоскую грань ($0.317).
Урок для `ss-series-starter`: **список канон-минимума проверяется не по объектам, а по
ПРОЧТЕНИЯМ.** Если эпизод показывает A, которое оказывается B, нужны две плиты — «как выглядит
A» и «как выглядит B», — и они не должны быть похожи.

**D27 🔴 (найден 31.07, открыт) — у агента НЕТ пути через приложение: закрыт весь конвейер, не одна ручка.**
D13 фиксировал `POST /api/assets/{id}/approve` → 307 на `/login`. Проверка headless-вызовом
показала, что это не отдельная ручка, а свойство всего слоя: `POST /api/series/{id}/bible`,
`POST /api/assets/{id}/approve`, `POST /api/episodes/{id}/regenerate-image-from-plan` — все три
отвечают 307. То есть агент физически не может ни завести канон, ни утвердить план, ни запустить
рендер тем же входом, которым это делает браузер. Открыт только событийный вход Inngest.
Следствие: каждый шаг зеркалится в базу скриптом, а зеркало неизбежно расходится с маршрутом
(маршрут пишет `activity_events.summary` — колонки с таким именем в базе нет).
Правка: сервисная авторизация для роли EXEC-DIR-AI (заголовок с ролевым токеном, как у
`team-chat-post.ts --as-exec-dir-ai`), либо явное признание, что путь агента — только события.

**D28 ✅ (найден и закрыт 31.07) — канон ГАРНИТУРЫ, прикреплённый как композиционный референс, рисует лист алфавита.**
`engraving_type` по своему тексту — специмен резчика: «ряды A–Z и 0–9, **не образующие слов**, на
изогнутой поверхности», плюс строка «кривизну наследует каждый последующий гравированный кадр».
Он был прикреплён к SH03 и SH06 через `props_in_frame`. Кадр, которому дают лист алфавита,
рисует лист алфавита — SH04 v01 пришёл с высеченными ABCDEF…0123456789 на конусе, SH03 — с
буквами «V I», не складывающимися в слово. Модель отработала точно по референсу; ошибка была в
том, ЧТО ей дали.
Закрыто заведением `inscription_panel` — канона ПРЕДЛОЖЕНИЯ (та же рука, реальные слова, плоская
кладка), собранного за $0 из уже утверждённого кадра SH06 v02, и отцеплением `engraving_type` от
всех шотов.
Урок: **канон гарнитуры и канон надписи — разные сущности.** Первый описывает, как режут буквы;
второй — что именно написано. В кадр идёт только второй. Обобщение для `ss-series-starter`:
у любого канона надо спрашивать «это описание СПОСОБА или описание ВЕЩИ?» — способ в кадр не
прикрепляется.

**D29 ✅ (найден и закрыт 31.07) — правка кадра не держится, пока посылка живёт в брифе.**
Дважды исправляли SH04 на уровне промпта, и дважды возвращалось требование «поверхность
генерится ЧИСТОЙ». Причина лежала выше: критерий приёмки в `SPC-brief` требовал ДВУХ языковых
версий фразы и выводил из этого — «значит оверлей, значит чистая поверхность, запечённая фраза
критерий не проходит». Стиль-канон серии держит ту же развилку явно: одна языковая версия —
пишем моделью, две и больше — чистим поверхность. Агенты вели себя правильно; неверна была
посылка, а её никто не оспаривал, потому что она стояла в приёмке.
Закрыто снятием требования перевода (решение Директора) и правкой брифа.
Урок: **если один и тот же дефект возвращается после починки, чинить надо не кадр, а источник,
из которого выводится требование.** Возврат дефекта — сигнал, что правка была ниже уровня его
рождения.

**D30 🔴 (найден 31.07, открыт) — визуальный критик не смотрит худшие клипы: фильтрует по статусу.**
`runVisualCriticForEpisode` прогнали по двум готовым клипам (SH01 и SH04). Он проверил ОДИН:
SH04 в статусе `REVISION` был молча пропущен — а именно он и был полным браком (снят со старого
референса: пирамида целиком плюс высеченный алфавит A–Z 0–9 вместо фразы). Получается обратная
зависимость: чем хуже кадр, тем вероятнее, что критик на него не посмотрит, потому что плохой
кадр уже помечен REVISION.
Правка: критик обязан оценивать последнюю версию клипа независимо от статуса — статус это
результат оценки, а не условие входа в неё.

**D31 ✅ (найден и закрыт 31.07) — абсолютная формулировка в continuity родила ложное срабатывание.**
Критик выдал REVISE на хороший клип SH01: «виден красно-оранжевый ободок на корпусе», сославшись
на пункт `continuity_notes` — «only two steady green nav points visible, **no red anywhere**».
Директор: запрет красного относится к НАВИГАЦИОННЫМ ОГНЯМ; янтарное свечение кабины — канон и
допускается везде. То есть критик отработал буквально по тексту, который мы ему дали, а текст
говорил больше, чем мы имели в виду.
Закрыто: шесть формулировок в раскадровке сужены с «no red anywhere» до «no red NAVIGATION point
(cabin amber glow permitted)».
Урок: **ложное срабатывание критика — дефект нашей формулировки, а не критика.** Абсолютные
слова («anywhere», «never», «no X at all») в контракте кадра надо сужать до того объекта, к
которому запрет реально относится, иначе критик честно забракует канон.
Отсюда же — разделение находок: критик ловит нарушения НАПИСАННОГО контракта, человек ловит
нарушения ЗАМЫСЛА (в SH01 в кадр вошла вершина формы — силуэт, который по замыслу не должен
раскрываться до SH06; в контракте этого пункта не было, и критик его не увидел).

**D32 🔴 (найден 31.07, открыт) — усиление атрибута превращает его в самостоятельный объект.**
Директор потребовал усилить навигационные огни: на референсах они бледные и при генерации видео
рискуют пропасть. Формулировку в контракте усилили по цвету — «дихроичный фонарь, самый
цветоплотный элемент кадра, читается зелёным даже точкой». В следующем же клипе SH06 модель
разместила два зелёных огня как **отдельные светящиеся объекты в открытой воде**: на шестой
секунде они стоят у противоположных краёв кадра, разнесённые на десятки длин корпуса, а сам
аппарат — точка между ними. К девятой секунде они сходятся к центру и читаются как **пара глаз
на зубе**.
Причина: усиление говорило только о ЦВЕТЕ и ЗАМЕТНОСТИ, ничего не говоря о ПРИНАДЛЕЖНОСТИ и
МАСШТАБЕ. Получив «самый заметный элемент кадра», модель повысила деталь корпуса до объекта
сцены — что для неё совершенно логично.
Закрыто добавлением якоря габарита в контракт всех шотов с аппаратом: «оба огня — арматура НА
КОРПУСЕ; расстояние между ними МЕНЬШЕ ДЛИНЫ аппарата; по мере удаления они сходятся в одну
неразрешимую точку, а не расходятся; если корпус не читается — огней не видно» плюс негатив
«никаких свободно висящих зелёных источников, никакой симметричной пары зелёных точек на камне».
Урок: **всякое усиление признака должно нести якорь принадлежности и масштаба.** Приказ «сделай
это заметнее» без «и вот чему оно принадлежит и какого оно размера» — это приказ повысить деталь
в объект. Тот же класс, что D28 (лист-образец гарнитуры дал в кадре лист алфавита): модель
исполняет буквально то, чем вещь ОБЪЯВЛЕНА, а не то, чем она подразумевалась.
