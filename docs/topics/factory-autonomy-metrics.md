# Тема: Метрики автономности фабрики

> Приборы НЕ нужны — разбивка уже в `activity_events.event_type`; настоящее число автономности 8–71 ручной пуск на эпизод; фаза 0 снята, прогон можно начинать; старые замеры времени невалидны.

*Потолок 80 строк — он же механизм удаления. Устаревшее выкидывать, не копить.
Доказательства живут в `docs/analysis/`, здесь только состояние темы.*

## РЕШЕНО

- **Провал — это только касание, ВЫПАДАЮЩЕЕ из цикла** (07-28). Директор крутит ручку,
  которую машина должна была повернуть сама. Пятьдесят плановых касаний — норма, не провал.
  Это уточняет метрику `PLANET.md` «Planned vs Unplanned» до проверяемого числа.
- **Цикл Директора v1 на эпизод ~20 шотов** (07-28): до старта референс-артиста ~10 (творческие
  гейты, не снимаются) · прокликивание референсов ~20 (**снимается** — видеокритик есть) ·
  прокликивание видеошотов ~20 (**снимается**) · выравнивание длительностей иногда · стич 1
  (не снимается) · дистрибуция 1 (**снимается** — unlisted без Директора, он смотрит и
  откатывает). Итого сейчас ~50, целевое на текущем стеке ~11.
- **Идеал студии — Директор утверждает ровно четыре вещи** (07-28): тему · бюджет · перечень
  тем эпизодов с расшифровками (draft → approved) · и всё. Совпадение: пункт 3 — это ровно
  уже построенная поверхность Themes (`SPC-theme_{slug}` + `metadata.theme_status`). Идеал не
  требует новой сущности, он требует, чтобы апрув темы ЗАПУСКАЛ цепочку.
- **Прибор УЖЕ в данных — `activity_events.event_type`** (07-28). В цикле:
  `approval_granted|revision|rejected`. Вне цикла (провал): `manual_trigger`,
  `episode_settings_changed`. Машина: `agent_started|completed|failed`. Замер по шести
  эпизодам без строчки нового кода: **ручных пусков 8–71 на эпизод** — вот настоящее число
  автономности. Директор фигурирует под ДВУМЯ идентификаторами (UUID и email) — считать оба,
  иначе теряется ~10 событий на эпизод. Остаток (~20 событий) не классифицирован — это
  доработка счётчика, не прибора.
- **Неоднозначность устраняется дизайном прогона, а не приборами** (07-28): Полина выключена ·
  Тео не трогает пайплайн · стек заморожен с объявленной версией · цикл объявлен списком
  заранее, поэтому всё сверх списка попадает в «вне цикла» диффом, без ручного счёта.
- **Ожидание Директора — настоящий драйвер календарного срока.** Разрывы между завершением
  гейтовой задачи и следующим человеческим событием; выводится из существующих таймстампов.
- **Три класса актора** (`director` · `polina` · `agent`) чинить всё равно надо — модель
  данных умеет (`created_by_kind`), дыра в роутах и `activity_events`. Но это гигиена, а не
  предусловие замера (см. ОПРОВЕРГНУТО).
- **Условие валидности прогона: во время прогона студию НЕ чиним.** Ломается — в журнал и
  дальше. Иначе снова замерим ремонт.
- **Переопределение метрик** (07-28): себестоимость — как есть, `budget_log` (единственная
  была валидна) · производственное время — СУММА длительностей задач, не окно первая→последняя ·
  касания — раздельно по трём классам, плюс плановые/выпадающие, считается только второе ·
  брак — с разделением отказ провайдера / отказ логики / REVISE критика.

## ОПРОВЕРГНУТО

- **«Скорость подтверждена наполовину: 7,7–50,4 часа, разброс шестикратный»** — замер
  невалиден (07-28). Мерилось wall-clock от первой до последней задачи эпизода, а в это окно
  у E01–E31 попадают дни отладки самой студии. 50 часов — шум капремонта на ходу, не
  производственная дисперсия.
- **«Касания Полины объясняют разрыв между оценкой ~50 и измеренным»** — снято тем же днём
  (07-28). Полина даёт **0–2 события на эпизод**, около процента. Лукавство в роутах
  (её `ToolContext` несёт auth user id Директора, роуты пишут `actor: user.id`)
  архитектурно реально, но эти цифры не искажает. Разрыв объясняет не она.
- **«Фаза 0 — приборы — предусловие замера»** — опровергнуто (07-28). Разбивка уже пишется,
  я читал сумму вместо неё. Прогон можно начинать сегодня.
- **«Около пятидесяти плановых касаний»** — занижено минимум для E27 и E28: одни только
  аппрувы дают 68–90 на эпизод.
- **«Производственная автономность готова»** — это вера, не факт (Директор 07-21: «мы так
  думаем, но не тестед»). Долг взят с открытыми глазами; закрывается первым чистым эпизодом
  бриф→финал с нулём незапланированных касаний.

## ОТКРЫТО

- **Фаза 1 — 3–4 эпизода Sandy на текущем стеке, быстро, руками.** Подтвердить себестоимость,
  касания и скорость на неломаной машине. Каждый эпизод с полным замером. **НЕ начата, но и
  НЕ заблокирована** — приборы оказались на месте.
- **Фаза 2 — вариация внутри схемы:** тот же герой, тот же движок, новый антураж + жёсткий
  хронометраж. Проверяет рычаг из `docs/topics/distribution-and-audience.md`.
- **Фаза 3 — склад идей.** Сделано 07-28: заготовки `SS-S18 Ancient`, `SS-S19 Other Species`,
  `SS-S20 Deep`, у каждой концепт-тема в `draft`, канал не привязан.

## КУДА СМОТРЕТЬ

- `docs/plans/director-cycle-and-episode-run.md` — цикл, метрики, план прогона целиком.
- `PLANET.md` → «Measure — NATURE of touch» и «Debt taken with eyes open».
