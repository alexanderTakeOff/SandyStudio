# Тема: Прогон через Полину

> Канал к Полине — `team-chat-post.ts --as-exec-dir-ai` (без флага откажет); ответы читаются из `concierge_turns` по таймстампу, не по строке; канон заводится маршрутами приложения, а не скриптом; в Mode 2 она переспрашивает КАЖДЫЙ изменяющий вызов.

*Потолок 80 строк — он же механизм удаления. Устаревшее выкидывать, не копить.
Заведено 30.07: каждая свежая сессия заново искала, как достучаться до Полины.
Порядок прогона и черновики скиллов — `docs/plans/series-bootstrap-run-notes.md`.*

## РЕШЕНО — плумбинг

- **Написать Полине** (cwd = `webapp`):
  `npx tsx scripts/team-chat-post.ts --file <msg.md> --as-exec-dir-ai [--thread <id>]`.
  Флаг обязателен: право едет на **ролевом токене EXEC-DIR-AI**, а не на подписи автора.
  Без него она вежливо отказывается исполнять — это не сбой, а работающий гейт.
- **Прочитать её ответ — готовый скрипт есть:** `npx tsx scripts/read-polina-turns.ts [N]`
  (по умолчанию 10 последних, целиком, без обрезки). Свой запрос к `concierge_turns` не писать.
- **Свои фильтры по времени сравнивать с таймстампом хода, а не со строкой-префиксом.**
  `created_at > '2026-07-30T08:1'` лексически больше `08:04:49` и молча съедает ответ. Ловилось дважды.
- **48 инструментов Полины** — список одной строкой:
  `grep -hoE "name: '[a-zA-Z_]+'" lib/concierge/tools/*.ts | sed "s/name: //" | tr -d "'" | sort -u`.
- **Полина на `gemini-2.5-flash` (free).** Оверрайд в `app_config` (`scope=providers`,
  `key=concierge_provider`), **НЕ** в `.env.local`. Обрыв её ответов лечится
  `CONCIERGE_AUTO_REACT_MAX_TOKENS` в `webapp/.env.local` (правится в `chat-internal/route.ts`,
  **не** в `/auto-react`). `prod.log` для её диагностики бесполезен — UTF-16 и только крон HoG.
- **MCP Supabase `execute_sql` запрещён** («You do not have permission»). База — только
  node/tsx-скриптом с ручным чтением `.env.local`.
- **Одноразовая диагностика — `scripts/diag-*.ts`**: уже в `.gitignore`. Не плодить `probe-*`,
  их там 14 и все мёртвые.
- **Стек — адрес НИКОГДА не вбивать руками.** `:3000/:8288` — порты МАСТЕРА; рабочее дерево
  живёт на `:3001/:8388`. Брать из `webapp/.env.local` (`NEXT_PUBLIC_APP_URL`, `INNGEST_BASE_URL`):
  стук в `:3000` даёт `000` и читается как «стек лежит» — ложный вывод, стоил хода 12.08.
  `inngest.log` содержит NUL-байты — читать через `tr -d '\000'`, иначе grep молчит.

## РЕШЕНО — чем управлять, а не что писать

- **Канон-ассет заводится маршрутами приложения, кода писать не нужно** (30.07). Цепочка для
  УЖЕ готового кадра: `POST /api/series/{id}/bible` (DRAFT + текст) →
  `POST /api/assets/{id}/upload` (multipart, поле `file`) →
  `npx tsx scripts/backfill-uploaded-canon.ts <assetId>` → `POST .../bible/{id}/lock`.
- ✅ **D7 ЗАКРЫТ 30.07** (`1bd4c4ad`): все три пути канон-картинки идут через один сборщик
  `assembleBibleImageRequest`, референсы объявляет автор строкой `Refs:` в блоке `## RENDER`.
- 🔧 **Правка канона — ШТАТНАЯ** (`802dcc10`): `enrichBible(assetId, notes)` — с `notes` писатель
  не сочиняет заново, а правит, текущая статья уходит ему неприкосновенной БАЗОЙ. Запись может
  объявить **саму себя** на строке `Refs:` — тогда прежний кадр служит базой и компоновка
  переживает правку. Четыре правки подряд, каждая с первого сообщения.
- 🚫 **«Возьми свой текст и замени в нём» по-прежнему НЕЛЬЗЯ** — вставала трижды за вечер (второй
  `getAsset` с той же подписью → сторожок, предел 6 раундов). Правка идёт `notes`, не просьбой.
- **Готовый файл с диска** становится референсом, только став ассетом: `bible` → `upload` →
  `backfill-uploaded-canon.ts`, дальше слаг объявляется в `Refs:`. Черновик тоже годится.
- **ЛОВУШКА: `upload` ставит `drive_file_id = null`.** Байты остаются локальными, и агентский
  media-preflight позже падает HALT'ом «Media unreachable» — так убился E16. Лечится не правкой,
  а третьим шагом цепочки: `backfill-uploaded-canon.ts` перекладывает файл через `persistBinary`
  в канонический кэш и Drive. Пропуск виден только на запуске агента, то есть поздно.
- `upload` / `regenerate-image` / `copyAssetImage` **отказываются работать по LOCKED** — поэтому
  LOCK всегда последним. `upload` сам бампает `image_prompt.current_version`, иначе браузер
  держит stale immutable-кэш.
- **Скрипт мимо приложения не оставляет следа** ни в `budget_log`, ни в матрице состояний, ни
  в Drive-раскладке. Директор поймал это на $1.58 первого мудборда — их до сих пор нет в леджере.
- **`images/generations` физически не тянет референс.** Каждый следующий ассет рисуется с
  чистого листа и расходится с предыдущим — это и есть давняя причина дрейфа канона Sandy.

## ОТКРЫТО

- **Стоячего одобрения на этапе завода СЕРИАЛА нет:** `updateWorkPlan` привязан к эпизоду, а
  эпизода ещё не существует. Поэтому в Mode 2 канон сериала это ~15 кругов подтверждений.
  Обход на 30.07 — гонять канон-блок в **Mode 3**, возврат в Mode 1 перед `createEpisode`.
  Деньги и видео остаются гейтом Директора в любом режиме. Долг приложения, не решение.
- **Два пути auto-react с одинаковой пометкой в метаданных:** клиентский `/auto-react` (без
  инструментов по дизайну) и серверный `chat-internal` (дисптач на строке 788, но с шапкой
  «no tool dispatch» — комментарий врёт). Дубль, долг.

## КУДА СМОТРЕТЬ

- **Возвращение в работу после очистки — `docs/plans/series-bootstrap-run-notes.md` §0:** там
  позиция прогона одной строкой, дальше §1 — первая незакрытая строка таблицы. Читать ЕЁ, а не
  восстанавливать состояние по коду: 30.07 на такое восстановление ушло сорок минут.
- `webapp/app/api/series/[id]/bible/` — три маршрута: create · regenerate-image · lock.
- Тред прогона «Глубины» 30.07: `647156a9-2c56-48f2-a678-4a4e95cb3367`.
