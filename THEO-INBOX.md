# THEO-INBOX — очередь Полины к Тео

> Заведён 2026-08-12 по указанию Директора: «пометь в коммитах для Тео — заведи такой
> файлик, он будет смотреть его регулярно хуком и брать в работу, чтобы не тормозить
> эпизод».

**Как этим пользуются.** Полина кладёт сюда то, что нашла во время прогона и чинить не
может или не должна — дефект инструмента, недостающее перо, врущая инструкция, правка в
sensitive-файле. Прогон при этом НЕ останавливается: она обходит и идёт дальше, а запись
остаётся здесь. Тео забирает пункты в работу и **удаляет закрытые** — файл живёт короткой
очередью, а не архивом. Каждый пункт несёт: дату, что именно, где (файл:строка), чем
обошли, и почему это не подождёт до следующего раза.

---

## ЗАКРЫТО 13.08 — вычеркнуто, чинить не нужно

Девять пунктов ушли из очереди сегодня. Список — чтобы никто не полез чинить сделанное;
подробности в коммитах и в `.claude/skills/ss-run/SKILL.md` §9.23–9.25.

| № | Чем закрыт |
|---|---|
| 1 · `set-status` снимал LOCK молча | `d0eae159` — замок выше инварианта слота: отказ с именем строки, снятие только `--unlock` и печатается отдельным решением |
| 2 · нет пера для текстовой плиты | `94f36d08` — `register-canon --text`: лист и тело плиты одним вызовом |
| 4 · `ss-run` бьёт в порты мастера | `9ecc1444` — рецепты читают `INNGEST_BASE_URL` из `.env.local` |
| 8 · `write-asset --version` затирал версию | `d0eae159` — названная версия заводит СОСЕДА; без неё DRAFT итерируется на месте |
| 12 · огни BOTTOM на турнаранде | `94f36d08` — v03 `d695139c`, свап каналов 3063 px, §5 переписан как замер. **Ждёт замка Директора** |
| 13 · кавычка обезоруживала ум | `a583433a` — PowerShell в `--allowedTools`, хард-лимиты держат обе руки. **Остаток:** сброс состояния Bash между ходами |
| 14 · кнопка исполняла чужую версию плана | `4658de06` — `assertPlanIsFreshAndExecutable`: свежесть + машиночитаемость, отказ до траты |
| 16 · `duration_seconds` — потолок или длина | `c0712333` — семантика в контракте, роут таймингов возвращает `effective_playback_seconds` |
| 17 · упаковка выпуска не подтягивалась | `c0712333` — `publish.ts` тянет `SPC-metadata`, ставит обложку и плейлист; `assertPublishable` бьёт по пустым секциям и кириллице |

---

## ОТКРЫТО

### 3 · `ensure-episode` рождает эпизод сразу в `BRIEF_APPROVED`
**12.08 · `webapp/scripts/run/ensure-episode.ts`**
Новый эпизод (`SS-S20-E07`, `4d1c0974`) получил статус `BRIEF_APPROVED` в момент создания —
брифа ещё нет, а гейт уже выглядит пройденным. Баннеры и гейты Директора читают статус, то
есть показывают неправду с первой секунды жизни эпизода.
**Надо:** рождать в `BRIEF_PENDING`.

### 5 · `cast-episode` не предъявляет строку, которую создал
**12.08 · E07 · `webapp/scripts/run/cast-episode.ts`**
Печатает `каст оформлен: asset ? · REVIEW` — вместо uuid стоит `?`. По закону студии
«сделано» предъявляется строкой; инструмент, который её не печатает, заставляет догонять
собственную работу отдельным вызовом `show-asset`. Строка нашлась —
`80bbaf10-063d-47c3-8826-4e138072e5f4`, — но искать её пришлось руками.

### 7 · Утверждение Директора в UI не доходит до ума — ни события, ни статуса
**12.08 · E07 · станция `casting`**
Директор утвердил каст в панели: строка `SS-S20-E07-SPC-episode_cast-v01-APPROVED.md`
(`80bbaf10-063d-47c3-8826-4e138072e5f4`) стоит APPROVED. **В тред эпизода не пришло
ничего** — ум узнал об этом, только когда Директор написал сам, и то проверкой `show`.
Статус эпизода при этом так и висит `BRIEF_APPROVED`, хотя станция каста пройдена.
**Класс, а не частный случай:** так закрывается ЛЮБОЙ гейт из UI — бриф, каст,
раскадровка, рефы. Утверждение брифа v02 сегодня прошло так же молча.
**Корень:** у ума нет входящего канала от базы. Ход ума рождается только репликой
Директора; изменение `assets.status`, сделанное руками в панели, никуда не стучится.
Отсюда же вторая половина: статус эпизода двигает только ум (D91), значит утверждение
мимо ума оставляет статус на прошлой станции — и баннеры показывают неправду.
**Канал УЖЕ ЕСТЬ — уточнение 12.08, тем же днём.** Через полчаса после этой записи в тред
сами пришли четыре `[ambient pipeline event · episode_settings_changed]` — Директор менял
потолки в панели, и ум узнал об этом без единого опроса. То есть шина до ума работает, а
подписано на неё только изменение НАСТРОЕК эпизода; смена `assets.status` не публикуется.
**И событие на утверждение ТОЖЕ есть — второе уточнение, тот же день.** При утверждении
раскадровки в тред пришло `[ambient pipeline event · approval_granted] APPROVE on
SS-S20-E07-STB-storyboard-v02-REVIEW.md`. То есть публикация решения Директора работает
штатно, и запись выше была шире факта.
**Что остаётся дефектом (сузить до этого):** на утверждении КАСТА события не пришло, хотя
строка встала в APPROVED. Значит дело не в отсутствии шины, а в том, что какой-то ОДИН
путь утверждения её не публикует — вероятно, каст утверждался не той же кнопкой, что
раскадровка. **Надо:** найти второй путь смены статуса в UI и подключить его к той же
публикации, что уже работает для `approval_granted`. Не строить ничего нового.
**Отдельно остаётся в силе:** статус ЭПИЗОДА не двигается ни при одном из путей — эпизод
прошёл каст, сценарий и раскадровку и всё ещё висит в `BRIEF_APPROVED` (пункт 9).

**РЕШЕНИЕ ДИРЕКТОРА 28.08 — как это чинить (ответ 112: чиним потом, но записано).**

> «"След" — это результат СОБЫТИЯ, "АВТОР СЛЕДА" — в подписи события. Если Полина приняла
> решение сама, то она автор следа. Если она просто поработала пультом и занесла моё
> указание, значит автор я, она только исполнитель.»

Отсюда форма починки, а не заплатка:

1. **Утверждение обязано рождать СОБЫТИЕ, а не менять поле.** Сегодня событие пишет только
   кнопка UI; `set-status` двигает `assets.status` молча, и в пайплайне следа нет. Значит
   `set-status` (и всякий будущий путь утверждения) идёт через тот же чокпоинт, что кнопка.
2. **У события ДВЕ подписи, не одна:** автор решения и исполнитель. `--reason` у
   `set-episode` уже несёт «где Директор это сказал» — тот же принцип распространяется на
   утверждение: указание Директора, занесённое умом, подписывается ДИРЕКТОРОМ, а решение,
   принятое умом самостоятельно, — умом.
3. **Различает их не догадка, а вход:** ход, пришедший репликой Директора (пульт, панель,
   `mind-say`), несёт его авторство; ход, начатый умом по своему суждению, — своё.

Пока не сделано, «утвердила сама» и «утвердил Директор дистанционно» в ленте неразличимы —
а это и есть вопрос, ради которого лента существует.

### 9 · Нечем двигать статус ЭПИЗОДА — при том что закон требует его двигать
**12.08 · E07 · `webapp/scripts/run/`**
Закон студии (D91): прошла станцию — двинь статус эпизода, баннеры и гейты Директора читают
статус, а не реплики. Инструмента для этого в инвентаре нет: `set-status` меняет статус
ИЗДЕЛИЯ, `sync-episode` сводит бюджет и раскадровку, `ensure-episode` только заводит строку.
Эпизод сегодня прошёл каст и сценарий и всё ещё висит в `BRIEF_APPROVED`.
**Связь с пунктом 7:** там сигнал не доходит от UI к уму, здесь ум не может ответить в
обратную сторону. Один и тот же разрыв, две стороны — чинить их стоит вместе.
**Надо:** либо тонкое перо `set-episode-status`, либо `sync-episode` выводит статус из
изделий (есть APPROVED-каст → станция каста пройдена) и двигает его сам.

### 10 · ✅ ЗАКРЫТ 28.08 — Сторож самосверки считал `--help` генерацией кадра
**Чинил Тео 28.08**, после того как дефект нашла ВТОРАЯ сессия независимо (Полина, E03) —
лучшее подтверждение, что он настоящий, а не разовая случайность.
**Правка:** `.claude/hooks/polina-stop.cjs:83` — из триггера гейта исключены команды с
`--help`/`-h`. Справка денег не стоит и изделия не рождает.
**Корень признан частично:** правильный триггер — факт траты или появление `IMG-*`, а не
текст команды. Хук в базу не ходит; исключение справки закрывает 100% наблюдавшихся ложных
срабатываний, остальное — когда у хука появится доступ к леджеру.
**Проверено поведением, а не чтением:** `--help` → не триггерит, реальный вызов → триггерит.

### 11 · Таблица цен в скилле `eref-designer` разошлась с реальностью на порядок
**12.08 · E07 · `.claude/skills/eref-designer/SKILL.md` §«Cost reference table»**
Таблица (снимок 18.05) обещает gpt-image-2: 1024×1792 — $0.030, 1024×1024 — $0.020, с
множителем ~1.10 за `openai-edits-multi`. Факт двух вызовов `gen-frame` сегодня:
**$0.317** за кадр (1024x1536, `high`, 2 рефа), обе строки в `budget_log`.
**Чем это плохо.** Цена называется Директору на гейте ДО траты — это и есть его основание
для «да». Я назвала $0.03–0.04 за кадр и ошиблась в девять раз. Ошибка безобидна при
двух пилотах и перестаёт быть безобидной на пятикадровом фан-ауте с перегенерациями.
**Корень:** цена живёт КОПИЕЙ в тексте скилла, а истина — в ответе провайдера и в
`budget_log`. Копия протухла молча, и ничто не имело права её опровергнуть.
**Надо:** убрать таблицу из скилла и заменить одной строкой «цену спрашивай у леджера»,
либо печатать в `gen-frame --help` фактическую цену последнего вызова этого размера и
тира. Второе дороже, первое честнее: меньше копий — меньше протухшего.

### 15 · Закон нарезки обошли через два исключения — текст правки готов, вставить в скилл
**12.08 · E07 · `.claude/skills/storyboard-shot-granularity/SKILL.md`**
Директор о собранном кате: «первый кадр и второй кадр с одного ракурса, фактически без
изменений, соединение выглядит как будто просто вырезка какая-то… нельзя так делать ни в
каких историях». Он же: «я где-то уже об этом говорил» — и он прав, закон в скилле СТОИТ с
31.07. Он не отсутствовал, его обошли двумя исключениями, и обошли письменно, в раскадровке.
**Полина править файл не может** — харнес блокирует скилл как sensitive.

**Вставить в §«The tests» после теста 4:**

> ### Two ways the three-axis test gets cheated (2026-08-12, Director)
>
> **Cheat 1 — «something APPEARS in the frame, so the object of attention changed».**
> It did not. A new element entering the DEPTH of a static frame — a light in the distance,
> a shape emerging from the dark — is an **event inside one shot**, not a change of vantage.
> The audience's eye has not been moved: same thing, same place, one more detail in it.
> That is the material a merged shot with internal phases is made of. The axis «object of
> attention» changes when the CAMERA changes what it points at, not when the world produces
> a new detail in the existing frame.
>
> **Cheat 2 — «the merged span exceeds the provider's ceiling, so I must cut».**
> The ceiling forces a SPLIT; it does not license a split **on the same vantage**. If the
> material must be divided for technical reasons, the second piece earns its cut the normal
> way: change the vantage, the scale, or the subject. The audience cannot see the provider's
> limits — only the seam.
> Order of operations: merge if the tests say merge → if it does not fit the ceiling,
> RESTAGE the second piece from a different camera → only then split. Never «same camera,
> cut in half because 18 seconds is more than 15».
>
> **The failure that closed these loopholes (SS-S20-E07, 12.08.2026):** an opening shot of a
> vehicle travelling away from camera, then the following shot — same vantage, same distance
> — where a point of light appears far ahead. Both loopholes were used to justify the cut,
> in writing, in the storyboard. In the assembled cut the join read as a missing piece of
> film rather than an edit.

**Почему это стоит вставить, а не пересказать:** правило уже один раз проиграло исключениям.
Пока исключения не названы поимённо, следующий прогон обойдёт его тем же способом.

### 6 · Спящий фолбэк на порт мастера в OAuth-редиректе
**12.08 · `webapp/app/api/channels/consent/callback/route.ts:70` и
`webapp/app/api/channels/[id]/consent/route.ts:46`**
`PUBLIC_ENV.APP_URL || 'http://localhost:3000'` — при пустом env редирект согласия уедет на
стек мастера. Сейчас спит (в `.env.local` адрес задан), поэтому не горит.
**Надо:** падать громко вместо фолбэка на чужой порт.

### 18 · `SPC-metadata` не охраняется инвариантом «один утверждённый на слот»
**13.08 · E07 · `webapp/lib/api/single-approved.ts:93-225`**
`resolveSlotDescriptor` знает слоты для `VID-animatic`, `IMG-thumbnail`, `IMG-episode_ref`,
`IMG-anchor`, `SPC-shot_plan`, `VID-shot`, `SPC-ref_plan` — и возвращает `null` для
`SPC-metadata`. Вытеснения нет: после утверждения v03 в базе висели ДВА APPROVED
(`2858e575` v01 и `9690d9e7` v03) одновременно. Оба живых читателя сортируют по свежести
(`runner.ts:3227` по version desc, шорт-роут по created_at desc) и взяли бы v03 — то есть
беда спит, но третий читатель без сортировки возьмёт что попало.
**Чем обошли:** сняла v01 и v02 руками через `set-status --status INVALIDATED`.
**Надо:** добавить `SPC-metadata` (а заодно проверить `SPC-copy`, `SPC-thumb_plan`,
`SCR-script`, `STB-storyboard` — у них слота тоже нет) в тот же список эпизодных слотов,
как это сделано для `VID-animatic`.

### 19 · Видимость выпуска нельзя выбрать в интерфейсе — только умолчание в коде
**13.08 · E07 · `webapp/lib/agents/runner.ts:3257`**
EXEC-PUB берёт видимость из `episodes.metadata.youtube_privacy`, а записать это поле в UI
негде: ни в карточке эпизода, ни в его настройках, ни на стадии Distribution переключателя
нет. Единственный селектор `privacy` в студии — в панели «Cut a short» под финальным катом,
и он управляет ТОЛЬКО ручной нарезкой шорта. Директор сегодня искал кнопку и не нашёл её —
кнопки не существует.
Пока спасает умолчание `?? 'unlisted'`: пустое поле = unlisted, что и требовалось. Но выбор
Директора нигде не выражается, и первый же выпуск, который должен уйти `public`, выяснит
это больно — либо уедет unlisted молча, либо потребует правки поля мимо интерфейса.
**Надо:** переключатель видимости там же, где Директор жмёт заливку, пишущий
`youtube_privacy` на эпизод; умолчание оставить `unlisted`.

### 20 · ДОКТРИНА · кнопка Директора = команда уму, а не исполнитель. Гейт EXEC-PUB надо не смягчать, а снимать
**13.08 · E07 · блокирует выпуск · выше пунктов 17-19 по важности**

Директор дословно (13.08, после того как гейт EXEC-PUB заблокировал заливку требованием
обложки, которой у вертикального Short не бывает):

> «по новой парадигме WEB UI это просто пульт управления и просмотровщик. эта кнопка должна
> просто давать тебе команду-одобрение от директора, а ты должна сама решать как технически
> выкладывать, если есть инструменты. то есть по факту по кнопке должен просто вызываться
> скилл дистрибуция — ты его читаешь, понимаешь как это происходит, и реализуешь. точка.
> В идеале так должны работать все наши функциональные блоки, если мы работаем по модели ai First»

Это не правка одного гейта, а закон о границе UI и ума — и он режет по-живому три места.

**1. Гейт EXEC-PUB не смягчать, а снимать.** Сегодняшний отказ («Thumbnail: need 1, found 0»)
— это машина, решающая ЗА ум, что нужно для выпуска, при том что исполнитель обложку берёт
best-effort (`runner.ts:3303`) и без неё прекрасно заливает. Условие «обложка нужна, если
доставка не shorts» — та же машина, только умнее; по доктрине решение о комплектности
принимает ум, читая скилл, а не таблица `required[]` в `gate.ts`. Требование гейта, которое
больше требования исполнителя, блокирует стадию, которая отработала бы, — этот же дефект в
`gate.ts` уже описан комментарием для `EXEC-THUMB-DESIGNER` и там исправлен удалением строки.

**2. Хук противоречит доктрине и обезоруживает ум.** `.claude/hooks/polina-pretool.cjs:26`
режет `scripts/run/publish.ts` **ВСЕГДА** с текстом «публикация — только кнопкой Директора в
webapp». Сегодня Директор дал прямую команду выложить прямо в треде — и ум всё равно не мог
её исполнить, потому что механика знает только «deny», а не «deny без команды». По новой
парадигме кнопка ДАЁТ команду, а исполняет ум: хук обязан пропускать заливку **при наличии
записанного одобрения Директора** и резать в его отсутствие. Хард-лимит от этого не слабеет
— разрешение по-прежнему исключительно Директорское, меняется только исполнитель.
**Надо:** кнопка пишет одобрение записью (эпизод + станция + время + кто), хук проверяет
запись вместо слепого deny. Пока этого нет, любая заливка идёт либо руками Директора, либо
никак — то есть ум на станции Distribution безрукий.

**3. Скилла дистрибуции, который тут «должен вызываться», НЕ СУЩЕСТВУЕТ.** В
`.claude/skills/` есть `head-of-growth` и `shorts-longform-distribution` — оба доктринальные
(зачем и куда), ни один не процедурный (как именно выкладывается выпуск: чем, в каком
порядке, что проверить до и после, что делать при отказе площадки). Кнопке нечего вызывать.
**Надо:** скилл `distribution` — процедура станции, по образцу `eref-designer` / `animator`:
инвентарь инструментов, порядок, гейты ДО траты, самосверка после заливки, поведение при
отказе. Пункт 17 (упаковка) при этом становится его частью, а не отдельной заплаткой.

**Сегодняшнее состояние E07:** всё, кроме кнопки, готово — финальный кат
`SS-S20-E07-VID-final_cut-v02-APPROVED.mp4` (32.5 с, 720×1280) и метаданные `9690d9e7`
(заголовок 73 символа, описание 517, 18 тегов, только латиница). Директор выбрал `043` —
ждать починки, обходы не городить. **Выпуск стоит.**
